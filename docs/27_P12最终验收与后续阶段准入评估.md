# P12 最终验收与后续阶段准入评估

> 日期：2026-09-11  
> 状态：**P12 Target Script / Localization 最终真实人工验收 PASS。**  
> 优先级：本文高于 `docs/01`、`docs/02`、`docs/25`、`docs/26` 以及 `AGENTS.md` 中所有仍将 P12 描述为“待真实人工验收 / TARGET_SCRIPT = PLANNED”的历史状态；P12 技术契约仍以 `docs/25_P12TargetScriptLocalizationProfessionalSkill与数据契约.md` 为实现依据。

---

# 1. 最终结论

用户已于 2026-09-11 对 P12 Target Script / Localization 明确确认：

```text
P12 PASS
```

因此正式状态升级为：

```text
TARGET_SCRIPT = AVAILABLE
P12 Target Script / Localization = 最终真实人工验收通过
```

这次 PASS 只确认 P12 已完成，不自动把后续 Target / Production 能力升级为 AVAILABLE。

当前继续保持：

```text
TARGET_ASSETS = PLANNED
TTS = PLANNED
TIMING = PLANNED
STORYBOARD = PLANNED
VIDEO_GENERATION = PLANNED
QC_SELECTION = PLANNED
LIP_SYNC = PLANNED
POST_PRODUCTION = PLANNED
```

---

# 2. 真实人工验收记录

本次验收确认：

- 真实 Provider 最终生成成功；在前两次严格契约失败后，第 3 次尝试通过严格结构化契约校验；
- `TARGET_SCRIPT` revision 1，状态 `CURRENT`；
- 2 集共 89 条目标对白；
- 89 条对白与 CURRENT Source Snapshot 中 89 条 canonical dialogue 在 `utterance_id`、顺序、时间、原文、语言上完全一致；
- 每条均包含 `translation_text / localization_text / final_target_dialogue`，无空值、重复或非法时间；
- CURRENT `SOURCE_VIDEO_SNAPSHOT + ADAPTATION_PLAN + TARGET_BIBLE` 三项输入 lineage 一致；
- provenance 与 Artifact Graph 完整；
- 未产生 Target Storyboard、TTS、Timing、Generation 等越界 Artifact；
- 英文对白整体自然，人物称谓、货币和美国文化语境与目标设定一致；
- 后端 P12 验收测试 7 项通过；
- 前端目标剧本展示测试 5 项通过；
- 普通项目页面可直接查看全部目标对白。

人工验收同时记录两处可接受但后续可继续留意的措辞：

1. `Nineteen ninety-nine.` 没有显式补出 `dollars`；
2. “不能进周家门”被本土化为不能参加 Miller 家庭活动。

两处均未改变当前叙事功能，不构成 P12 阻断项。

---

# 3. P12 最终基线

Professional Skill / 运行契约继续保持：

```text
target-script-localization@1.0.0
p12-target-script-localization-v1
TARGET_SCRIPT schema 1.0
replica-target-script-localization-v1
p6-canonical-dialogue-frozen-in-p10-v1
```

正式硬输入继续固定为：

```text
CURRENT SOURCE_VIDEO_SNAPSHOT
CURRENT ADAPTATION_PLAN
CURRENT TARGET_BIBLE
```

Source Dialogue 继续只允许来自：

```text
SOURCE_VIDEO_SNAPSHOT.episodes[*].canonical_dialogue[*]
```

Provider 无权重新 ASR / OCR / 听写 / 看口型猜词 / 剧情补词 / 静默修正 Source text，也不得修改 canonical timing 或 utterance 集合。

正式对白链继续固定为：

```text
Source Dialogue
→ Translation
→ Localization
→ Final Target Dialogue
→ Target Speaker/Voice
→ TTS
→ Actual Speech Duration
→ Timing Plan
```

P12 只负责到 `Final Target Dialogue`。

---

# 4. P12 PASS 后的能力状态

正式能力状态更新：

```text
LOCALIZATION = AVAILABLE
TARGET_BIBLE = AVAILABLE
TARGET_SCRIPT = AVAILABLE
```

`TARGET_SCRIPT = AVAILABLE` 表示 P12 已完成自身真实人工验收，可以作为后续正式 CURRENT Target Artifact 被消费。

它不表示：

- Target Character / Scene / Prop 资产已经完成；
- Target Voice 已完成；
- TTS / Actual Speech Duration 已完成；
- Timing Plan 已完成；
- Target Storyboard 已完成；
- Generation / QC / Selection / Post 已完成。

---

# 5. 后续阶段准入评估

当前 Root Replica Skill 在 `target_script` 之后的下一个内部步骤是：

```text
target_assets
```

其当前能力仍为：

```text
TARGET_ASSETS = PLANNED
```

仓库在本次 P12 PASS 时**尚没有一个正式编号的 P13 Target Assets 数据契约 / Professional Skill / 人工验收规范**。因此本次状态提交只完成 P12 收口与 `TARGET_SCRIPT = AVAILABLE`，不把 Root Skill 中的步骤顺序擅自解释为“P13 已正式准入”。

进入下一阶段前必须先按现有开发规则：

1. 明确下一工程切片的正式名称与边界；
2. 先写正式契约文档；
3. 明确硬输入、typed output、Artifact Graph、Provider / deterministic 责任边界；
4. 明确不得提前进入的后续能力；
5. 再实现代码与真实人工验收。

在下一份正式契约出现前，禁止因为 P12 PASS 顺手实现 `TARGET_ASSETS / TTS / TIMING / STORYBOARD / GENERATION / QC / POST`。

---

# 6. 状态口径

从本文起，以下旧口径作废：

```text
P12 待真实人工验收
TARGET_SCRIPT = PLANNED
```

正式当前口径为：

```text
P12 PASS
TARGET_SCRIPT = AVAILABLE
下一工程阶段待正式契约定义
```
