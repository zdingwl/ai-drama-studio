# AI Drama Studio V3 开发阶段与验收清单

> 更新时间：2026-09-10  
> 依据：`docs/00_V3产品与系统详细规划.md`。  
> 原则：一次完成一个可真实验收的纵向切片，不同时铺开六条半成品流程。  
> P5 / P6 / P7 / P8 / P9 是工程切片；普通用户统一看到“原片理解”业务阶段。

---

# 当前总览

```text
P0 仓库重建基线            ✅
P1 新工程骨架              ✅
P2 Project + Skill Kernel  ✅
P3 SourceAsset + 输入系统  ✅
P4 Task / ProviderJob      ✅
P5 镜头技术锚点            ✅
P6 Source Evidence         ✅
P7 整集多模态原片理解      ✅
P8 逐镜精细拉片            ✅
P9 Speaker/Character/Scene/Prop 归一  🟡 工程实现完成，待真实验收
P10 SourceVideoSnapshot    ⛔ P9 真实验收完成前禁止进入
```

P7 最终验收以 `docs/07_P7最终验收与ProviderReadiness.md` 为准。  
P8 最终验收与 P9 准入以 `docs/13_P8最终验收与P9准入.md` 为准。  
P9 工程契约以 `docs/14_P9最终归一ProfessionalSkill与数据契约.md` 为准。

---

# P0 仓库重建基线 ✅

- [x] 清空旧 main；
- [x] 建立回退分支；
- [x] 写 V3 产品总纲；
- [x] 写开发规则；
- [x] 写阶段验收清单。

---

# P1 新工程骨架 ✅

后端：FastAPI / Pydantic Settings / SQLAlchemy / Alembic / pytest / FFmpeg 基础集成。

前端：Vue 3 / TypeScript / Pinia / Router / Vite / Vitest / API client。

CI：backend install / compile / FastAPI import / migrate / pytest；frontend typecheck / unit / build。

---

# P2 Project + Skill Kernel ✅

已建立：

```text
Project Type
→ Root Skill
→ Plan Compiler
→ ProjectExecutionPlan
→ Professional Skill / Capability
→ Artifact Graph
```

已验收：

- [x] 六种 `ProjectType`；
- [x] Root Skill / manifest / SKILL.md；
- [x] Capability Registry；
- [x] `PLANNED / AVAILABLE`；
- [x] ProjectExecutionPlan 持久化 / fingerprint / revision；
- [x] Artifact revision / CURRENT / STALE；
- [x] ArtifactRelation 与 Source / Target / Production guardrail；
- [x] GET `/plan` 只读，compile 只能显式 POST；
- [x] Product Stage 与内部 PlanStep 分离。

---

# P3 SourceAsset + 输入系统 ✅

已验收：

- [x] immutable SourceAsset；
- [x] 视频 Episode / order；
- [x] ffprobe / decode preflight；
- [x] SOURCE_VIDEO revision / fingerprint / CURRENT / STALE；
- [x] 文本 SourceDocument / SOURCE_TEXT；
- [x] 输入变化清除 current plan pointer；
- [x] GET 不自动重编 Plan。

完整 Episode 从本阶段开始即为视频 Source Truth。

---

# P4 Task / ProviderJob ✅

已验收：

- [x] Task explicit command；
- [x] Idempotency-Key / fingerprint；
- [x] queue / progress / heartbeat / checkpoint；
- [x] finite retry / cancel / resume；
- [x] safe last_error；
- [x] ProviderJob 在外部计费请求前先 commit；
- [x] ProviderJob 不保存 API Key / Authorization / 原始业务 payload；
- [x] Task 技术成功不等于业务 Artifact READY；
- [x] 失败不覆盖旧 CURRENT Artifact。

---

# P5 镜头技术锚点 ✅

正式范围：

```text
完整 Episode
→ Shot Boundary
→ thumbnail
→ Reference Clip
→ SHOT_ANCHORS Artifact
```

已验收：

- [x] 真实 Episode 约 1:06；
- [x] 成功生成 28 个镜头；
- [x] Shot 有序、不重叠、覆盖 Episode；
- [x] thumbnail / Reference Clip 可见可播；
- [x] Source 变化后旧 SHOT_ANCHORS STALE；
- [x] GET read-only；
- [x] Reference Clip 仅作派生核对资产，不替代完整 Episode。

`SHOT_BOUNDARY = AVAILABLE`。

---

# P6 Source Evidence ✅

固定输入契约：

```text
完整 SOURCE_VIDEO
├─ ASR：连续读取完整 Episode 音轨
└─ OCR：扫描完整 Episode 视频时间轴
```

Shot Anchors 仅可辅助 OCR 抽帧 / 去重和对白时间投影，不能成为 P6 Source Truth。

当前已验收并进入基线：

- [x] ASR 不依赖 SHOT_ANCHORS 才能开始；
- [x] ASR 不按 Reference Clip 分开识别后拼句；
- [x] canonical SourceDialogueUtterance；
- [x] 跨 Shot 一句话仍是一条 canonical dialogue；
- [x] OCR canonical visual text span；
- [x] SourceEvidenceSet / provenance / fingerprint；
- [x] 项目级 SOURCE_DIALOGUE revision / CURRENT / STALE；
- [x] raw ASR/OCR 保留；
- [x] micro-duplicate canonical admission guard；
- [x] OCR subtitle near-match 可追溯自动裁决；
- [x] 用户可显式选择 ASR / 重叠 OCR / 自定义文本并生成新 revision；
- [x] VLM / P7 / P8 无权静默覆盖 canonical Evidence；
- [x] 用户真实短剧人工验收通过。

当前自动基线：

```text
p6-source-evidence-v4
segment-preserving-dialogue-v4
adjacent-duplicate-microsegment-v1
ocr-subtitle-near-match-v1
human-dialogue-adjudication-v1
```

`SOURCE_DIALOGUE_EVIDENCE = AVAILABLE`。

---

# P7 整集多模态原片理解 ✅

P7 是 Source Understanding 语义层，不替代 P6 Evidence。

固定输入：

```text
完整 immutable Episode
+
CURRENT P6 Source Evidence
+
可选 CURRENT Shot Anchors
```

正式 Professional Skill：

```text
source-video-understanding@1.1.0
```

正式运行契约：

```text
p7-source-bible-v2
SOURCE_BIBLE schema 1.1
grounded-source-truth-v2
```

正式输出：

```text
SOURCE_BIBLE《源作概览分析》
+
Story Skeleton
+
Rhythm Skeleton
```

SOURCE_BIBLE 至少覆盖：

- [x] Material Baseline；
- [x] 整体故事分析；
- [x] 时间化原作剧情；
- [x] 人物 / 状态 / 关系；
- [x] 场景；
- [x] 关键道具；
- [x] Story Events / Beats；
- [x] Story Skeleton；
- [x] Rhythm Skeleton；
- [x] claim grounding；
- [x] revision / provenance。

Grounding：

```text
FACT
INFERENCE
UNKNOWN
```

已验收：

- [x] FACT / INFERENCE 必须有 Evidence 或完整视频时间支持；
- [x] UNKNOWN 不得作为确定性正文的逃逸通道；
- [x] world_rules 只允许片内 FACT，没有时允许空；
- [x] 道具外观与剧情作用分离；
- [x] Provider + Service publication 双层 fail-closed；
- [x] hallucinated Evidence ID / 越界时间 / 无效人物引用拒绝发布；
- [x] Evidence 短引用映射后恢复 canonical ID；
- [x] SOURCE_BIBLE 可读、可编辑、编辑产生新 revision；
- [x] 上游 / Provider profile 变化正确 STALE；
- [x] P6 canonical Evidence 始终不被改写；
- [x] Doubao Seed 2.1 Pro 真实 Ark + 真实短剧端到端成功；
- [x] 最终 Grounding v2 结果通过人工内容核对。

Capability：

```text
EPISODE_UNDERSTANDING = AVAILABLE
STORY_RHYTHM          = AVAILABLE
```

---

# P8 带 Source Bible 的逐镜精细拉片 ✅

固定输入：

```text
完整 Episode
+
CURRENT SOURCE_BIBLE
+
CURRENT SHOT_ANCHORS
+
CURRENT canonical Source Evidence
↓
逐镜精细拉片
```

正式契约：

```text
shot-breakdown@1.1.0
p8-shot-breakdown-v2
SOURCE_SHOT_FACTS schema 1.1
source-bible-shot-facts-v2
```

已完成并最终验收：

- [x] `shot-breakdown` Professional Skill；
- [x] Shot-level typed artifact / domain schema；
- [x] visual description；
- [x] 景别 / 构图 / 镜头类型 / 运镜 / 焦距景深；
- [x] 人物 / 场景 / 道具 candidate binding；
- [x] canonical dialogue / voiceover Shot binding；
- [x] speaker candidate hint；
- [x] 同一 canonical utterance 跨 Shot 保持同一说话人候选和连续关系；
- [x] 音效 / 环境声描述边界；
- [x] Source Bible 全局上下文注入；
- [x] Shot Anchors 边界保持；
- [x] Reference Clip 仅用于局部精看；
- [x] CURRENT / STALE / fingerprint / provenance；
- [x] ProviderJob 先持久化；
- [x] GET read-only；
- [x] 用户可读逐镜分镜表；
- [x] 真实短剧 28 Shot 工程 / Provider / 数据一致性 / 最终人工音画验收。

最终真实基线：

```text
P5 SHOT_ANCHORS      rev1 CURRENT
P6 SOURCE_DIALOGUE   rev9 CURRENT
P7 SOURCE_BIBLE      rev8 CURRENT
P8 SOURCE_SHOT_FACTS rev6 CURRENT
```

最终 Scene binding：

```text
#001       王桂香家客厅
#002-#009  居民楼公共楼道
#010-#028  徐然家客厅
```

28/28 Shot 时间与 P5 一致；P8 dialogue 与 P6 canonical 逐字差异 0；candidate / provenance / fingerprint / Artifact Graph / SUPERSEDES 均通过最终复核。

因此：

```text
SHOT_BREAKDOWN = AVAILABLE
```

最终记录见 `docs/13_P8最终验收与P9准入.md`。

---

# P9 Speaker / Character / Scene / Prop 最终归一 🟡

P9 的设计、后端、Provider 编排、Artifact、人工裁决和 UI 已完成工程实现并通过自动化门禁；真实短剧 Provider / 真实数据库 / 最终人工音画验收尚未执行，因此 P9 仍未正式收口。

硬输入：

```text
CURRENT SOURCE_VIDEO / 完整 Episode
+
CURRENT SOURCE_BIBLE
+
CURRENT SOURCE_SHOT_FACTS
+
CURRENT SHOT_ANCHORS
+
需要核对时读取 CURRENT canonical Source Evidence
```

正式工程契约：

```text
character-resolution@1.0.0
speaker-attribution@1.0.0
scene-resolution@1.0.0
prop-resolution@1.0.0
p9-source-resolution-v1
P9 resolution schema 1.0
full-episode-global-resolution-v1
```

正式 Source outputs：

```text
SOURCE_CHARACTERS
SOURCE_SPEAKERS
SOURCE_SCENES
SOURCE_PROPS
```

已完成工程门禁：

- [x] `character-resolution` Professional Skill；
- [x] `speaker-attribution` Professional Skill；
- [x] `scene-resolution` Professional Skill；
- [x] `prop-resolution` Professional Skill；
- [x] P7/P8 provisional candidate → stable Source identity 的规则；
- [x] Speaker 与 Character 分层建模，Speaker→Character 可空且不默认一一对应；
- [x] UNKNOWN / UNRESOLVED 为一等正式状态；
- [x] Provider 以完整 Episode / 全集批次执行，支持跨 Shot、必要时跨 Episode evidence；
- [x] 同名人物、相似外观人物不会由服务端自动 merge；
- [x] Scene 同地点跨 Shot merge / 同名不同地点 split 契约；
- [x] Prop 同实例连续性 merge / 相似不同实例防误合并契约；
- [x] typed schema / domain model；
- [x] `SOURCE_SPEAKERS` 独立 Source Artifact 类型；
- [x] 四类 Artifact revision / fingerprint / provenance / CURRENT / STALE；
- [x] Artifact Graph 与 `SUPERSEDES`；
- [x] Character → Speaker 的 `USES` 依赖，Character 改版会使旧 Speaker STALE；
- [x] GET read-only；
- [x] 重任务显式 POST / Idempotency / retry；
- [x] 四个外部 ProviderJob 均通过统一机制在远程调用前先持久化；
- [x] P5 Shot 时间和 P6 canonical text/time 服务端强校验且无修改路径；
- [x] CONFIRM / MERGE / SPLIT / MARK_UNKNOWN / REASSIGN 版本化人工裁决；
- [x] 人工 split / reassign 后重新计算实体成员并重验 CURRENT P5/P6/P8 引用；
- [x] 任一四 Artifact 发布失败时 fail closed，不留下半套 CURRENT；
- [x] P9 工作区 UI：四类结果、未决状态、provenance/revision、显式重跑、人工裁决；
- [x] Alembic `0016_p9_source_resolution`；
- [x] V3 CI：backend compile/import/migrate/pytest 与 frontend typecheck/unit/build 全部通过。

尚未完成、不得冒充 PASS：

- [ ] 使用真实短剧和真实外部 Provider 运行 P9 四个 Professional Skill；
- [ ] 在真实数据库核对四类 Artifact / revision / fingerprint / provenance / Artifact Graph；
- [ ] 逐条人工核对 Speaker 与 CURRENT P6 canonical utterance 的音画归因；
- [ ] 按 P8 Scene 基线核对 P9 Scene merge，并真实验证一次同名场景 split；
- [ ] 真实验证关键 Prop 同实例 merge 与相似不同实例不误合并；
- [ ] 真实执行至少一次 merge/split 与一次 Speaker→Character 重新裁决，确认旧 revision 保留和下游 STALE；
- [ ] 最终人工音画验收通过；
- [ ] 验收后再评估 P9 capabilities 是否可从 `PLANNED` 升级为 `AVAILABLE`。

P9 开发 / 验收期间继续保持：

```text
IDENTITY_RESOLUTION = PLANNED
SCENE_RESOLUTION = PLANNED
PROP_RESOLUTION = PLANNED
SOURCE_SNAPSHOT = PLANNED
```

不得提前实现或完成 P10 `SOURCE_SNAPSHOT`。

---

# 下一步

只执行 P9 真实验收，不进入 P10：

```text
1. 使用当前最终真实 Source 链运行 P9 真实 Provider
2. 核对 SOURCE_CHARACTERS / SOURCE_SPEAKERS / SOURCE_SCENES / SOURCE_PROPS
3. 执行 merge/split 与 Speaker→Character 人工裁决
4. 核对 revision / fingerprint / provenance / Artifact Graph / STALE / GET 无副作用
5. 完成最终人工音画验收
```

P9 真实验收全部 PASS 前，`IDENTITY_RESOLUTION / SCENE_RESOLUTION / PROP_RESOLUTION` 保持 `PLANNED`，禁止进入 P10。
