# AI Drama Studio V3 开发阶段与验收清单

> 版本：V3.2 Source Input Baseline  
> 日期：2026-09-08  
> 依据：`00_V3产品与系统详细规划.md`。  
> 原则：一次完成一个可真实验收的纵向切片，不同时铺开六条半成品流程。

---

# P0 仓库重建基线 ✅

- [x] 清空旧 main；
- [x] 建立回退分支；
- [x] 写 V3 产品总纲；
- [x] 写开发规则；
- [x] 写阶段验收清单。

---

# P1 新工程骨架 ✅

## 后端

- [x] FastAPI；
- [x] Pydantic Settings；
- [x] SQLAlchemy 2.x；
- [x] Alembic；
- [x] pytest；
- [x] 健康检查；
- [x] 统一错误响应；
- [x] UTC 时间策略；
- [x] Artifact 根目录。

## 前端

- [x] Vue 3；
- [x] TypeScript；
- [x] Pinia；
- [x] Vue Router；
- [x] Vite；
- [x] Vitest；
- [x] API client；
- [x] 基础布局。

## CI

- [x] backend install / compile / import / pytest；
- [x] frontend install / typecheck / test / build；
- [x] V3 CI 全绿。

---

# P2 Project + Skill Kernel ✅

P2 已建立正式内核：

```text
Project Type
→ Root Skill
→ Plan Compiler
→ ProjectExecutionPlan
→ Artifact Graph
```

## Project

- [x] 六种 `ProjectType`；
- [x] source_language；
- [x] target_language；
- [x] target_region；
- [x] scene_strategy；
- [x] audio_policy；
- [x] visual_style；
- [x] root_skill_id / root_skill_version；
- [x] workflow_revision；
- [x] current_plan_id；
- [x] UTC timestamps；
- [x] Alembic migration。

六种类型：

```text
REPLICA
REDRAW
TRANSLATION
NOVEL_TO_DRAMA
SCRIPT_TO_DRAMA
SCRIPT_LOCALIZATION
```

## Skill / Capability

- [x] `SkillManifest` schema；
- [x] `SkillRegistry`；
- [x] manifest + `SKILL.md`；
- [x] Skill version / category；
- [x] when_to_use / when_not_to_use；
- [x] required_inputs / readable_artifacts；
- [x] required_capabilities / subskills；
- [x] output_contracts / completion_criteria / failure_policy；
- [x] 六个 Root Project Skill；
- [x] `CapabilityRegistry`；
- [x] `PLANNED / AVAILABLE` 能力状态；
- [x] Skill 只依赖 capability，不绑定具体模型。

Root Skill 存放：

```text
skills/projects/*/manifest.json
skills/projects/*/SKILL.md
```

Replica Root Skill 已明确锁定：

```text
故事骨架 Story Skeleton
+
节奏骨架 Rhythm Skeleton
```

## Artifact Graph

- [x] artifact_type；
- [x] revision；
- [x] input_fingerprint；
- [x] producing skill id / version；
- [x] CURRENT / STALE；
- [x] Source / Target / Production namespace；
- [x] ArtifactRelation；
- [x] 不存在节点引用拒绝；
- [x] 跨项目关系拒绝；
- [x] Target / Production 反向写 Source 拒绝；
- [x] 上游新 revision 使旧版本及依赖下游 STALE。

## ProjectExecutionPlan / Compiler

- [x] Plan 与 PlanStep 持久化；
- [x] plan revision；
- [x] plan input fingerprint；
- [x] root skill version；
- [x] required capability；
- [x] dependency / expected outputs；
- [x] current plan pointer；
- [x] 同输入重复 compile 幂等；
- [x] 不同 ProjectType 计划不同；
- [x] 配置变化使旧计划失效；
- [x] Artifact 变化使旧计划失效；
- [x] `GET /plan` 只读；
- [x] compile 必须显式 POST Command。

## P2 API / 前端

- [x] Skill / capability APIs；
- [x] Project CRUD；
- [x] Plan GET + compile command；
- [x] Artifact / Artifact Graph GET；
- [x] 六张“创建什么”项目类型卡；
- [x] 固定枚举使用下拉框；
- [x] 工作区读取后端 Plan；
- [x] 前端不维护独立 stage graph；
- [x] 未实现能力显示“等待后续能力接入”。

## P2 真实 CI

```text
Run: 34195020700
Head: e594a9d3479152ef9dd2733770b3d8d3c662789f
Conclusion: success
```

---

# P3 SourceAsset + 输入系统 ✅

P3 的目标：让真实用户素材安全进入正式 Artifact Graph，并且后续工作流能够感知版本变化。

## P3.1 通用 SourceAsset

- [x] `SourceAsset`；
- [x] original filename；
- [x] MIME；
- [x] size_bytes；
- [x] SHA256；
- [x] VIDEO / TEXT 类型；
- [x] 安全路径；
- [x] 路径穿越拒绝；
- [x] immutable raw source；
- [x] 流式写盘；
- [x] 文件大小限制；
- [x] 同项目 / 同类型 / 同 SHA256 上传幂等；
- [x] Artifact Graph binding；
- [x] SourceAsset 与业务 `SOURCE_VIDEO / SOURCE_TEXT` Artifact 分离。

原始文件正式原则：

```text
SourceAsset = 不可变原始文件
SOURCE_VIDEO / SOURCE_TEXT Artifact = 当前项目输入事实
```

禁止覆盖原文件来模拟“修改”。

## P3.2 视频项目

适用：

```text
REPLICA
REDRAW
TRANSLATION
```

- [x] `Episode`；
- [x] 多视频上传；
- [x] 上传顺序成为初始 Episode order；
- [x] Episode order 持久化；
- [x] 前端拖动排序；
- [x] 后端完整排序校验；
- [x] 排序变化生成新 `SOURCE_VIDEO` revision；
- [x] ffprobe；
- [x] duration / width / height；
- [x] codec / avg frame rate；
- [x] audio presence；
- [x] FFmpeg decode preflight；
- [x] 无视频轨明确失败；
- [x] 损坏视频明确失败；
- [x] MP4 / MOV / MKV / WebM / AVI / M4V 首版支持。

普通 UI 只显示：

```text
集数
文件名
时长
顺序
```

内部 SHA256、ffprobe JSON、磁盘路径不展示给普通用户。

## P3.3 文本项目

适用：

```text
NOVEL_TO_DRAMA
SCRIPT_TO_DRAMA
SCRIPT_LOCALIZATION
```

- [x] TXT；
- [x] Markdown / `.md` / `.markdown`；
- [x] `SourceDocument`；
- [x] UTF-8 / UTF-8 BOM；
- [x] 非 UTF-8 明确失败；
- [x] 空文本明确失败；
- [x] document revision；
- [x] current document pointer 语义；
- [x] 相同当前文本重复上传幂等；
- [x] 替换文本生成新 revision；
- [x] 旧 `SOURCE_TEXT` Artifact STALE；
- [x] 前端显示文件名 / 当前 revision / 字符数。

DOCX / PDF 不属于 P3，留给后续 Document Extractor 扩展。

## P3.4 Artifact / Plan 联动

- [x] 当前 Episode 集合聚合为 `SOURCE_VIDEO` Artifact；
- [x] Episode 顺序参与 input fingerprint；
- [x] 当前 SourceDocument 聚合为 `SOURCE_TEXT` Artifact；
- [x] Source 输入改变会产生新 Artifact revision；
- [x] 旧 Source Artifact 自动 STALE；
- [x] 依赖旧 Source 的下游沿 ArtifactRelation STALE；
- [x] Source 变化清除当前 Plan pointer；
- [x] GET 不自动重编计划；
- [x] 用户通过显式 command 按新 Source 状态重编 Plan。

## P3.5 API

- [x] `GET /api/v3/projects/{id}/sources/assets`；
- [x] `POST /api/v3/projects/{id}/sources/videos`；
- [x] `GET /api/v3/projects/{id}/sources/episodes`；
- [x] `POST /api/v3/projects/{id}/sources/episodes/reorder`；
- [x] `POST /api/v3/projects/{id}/sources/document`；
- [x] `GET /api/v3/projects/{id}/sources/document`。

## P3.6 前端

- [x] 视频项目显示“上传原片”；
- [x] 支持一次选择多个视频；
- [x] 显示 Episode 列表；
- [x] 支持拖动排序；
- [x] 文本项目显示“上传文本 / 替换文本”；
- [x] 显示当前文本 revision；
- [x] 素材变化提示重新生成执行计划；
- [x] 不暴露内部存储与媒体诊断信息。

## P3 必测

- [x] 批量 Episode 保持用户上传顺序；
- [x] 真实 FFmpeg 生成测试视频；
- [x] ffprobe 读取真实视频；
- [x] decode preflight；
- [x] 视频损坏明确失败；
- [x] 视频重复上传幂等；
- [x] 原始文件字节保持不变；
- [x] 拖动排序对应后端排序契约；
- [x] 排序后 `SOURCE_VIDEO` revision +1；
- [x] 文本 revision；
- [x] 文本重复上传幂等；
- [x] 非 UTF-8 明确失败；
- [x] 路径穿越拒绝；
- [x] 视频 / 文本项目类型边界拒绝错误输入；
- [x] Source 变化后旧 Plan 失效；
- [x] Plan 可以按新 Artifact 状态显式重新编译。

## P3 真实 CI

```text
Run: 34197132245
Head: 0afe2c671f36d2e754333386b9a8c198220ef0db
Conclusion: success
```

通过：

```text
FFmpeg runtime
backend install
backend compileall
FastAPI import
Alembic 0001 → 0002 → 0003
backend pytest: 29 passed
frontend install
frontend typecheck
frontend unit test
frontend build
```

P3 真实素材层级目前是“CI 动态生成真实可解码视频 + 真实 ffprobe/decode”，还没有拿用户正式长短剧项目做人工媒体验收。后续进入原片分析链时继续补真实项目验收。

---

# P4 Workflow / Task / ProviderJob ▶ 当前阶段

P4 必须在任何真实 ASR / OCR / Step 3.7 / H3 Provider 接入前完成。

## Task

- [ ] explicit command create；
- [ ] `Idempotency-Key`；
- [ ] input fingerprint；
- [ ] queue；
- [ ] worker；
- [ ] status；
- [ ] started_at / finished_at；
- [ ] heartbeat；
- [ ] checkpoint；
- [ ] retry limit；
- [ ] finite retry；
- [ ] cancel；
- [ ] interrupted resume；
- [ ] last_error；
- [ ] 同一业务输入不会重复创建失控任务。

## ProviderJob

- [ ] 外部 / 计费 Provider 请求前先持久化；
- [ ] capability；
- [ ] project / episode / artifact scope；
- [ ] provider；
- [ ] model；
- [ ] payload fingerprint；
- [ ] local job id / remote job id；
- [ ] status / attempt；
- [ ] started_at / finished_at；
- [ ] safe error；
- [ ] 不保存 API key；
- [ ] 不保存 Authorization header；
- [ ] 不把敏感请求内容写进日志。

强制顺序：

```text
创建 ProviderJob
↓
数据库提交成功
↓
才允许真正调用远端 / 计费 Provider
```

## Guardrail

- [ ] GET read-only；
- [ ] Skill 无权绕过 ProviderJob；
- [ ] Provider Adapter 无权先远端调用后补 Job；
- [ ] Skill 无权消费 STALE Artifact；
- [ ] Task 技术完成不等于业务 Artifact READY；
- [ ] Task 失败不静默覆盖旧 CURRENT Artifact；
- [ ] cancel / retry / resume 有确定性边界。

## P4 验收

- [ ] GET 路由不会创建 Task / ProviderJob；
- [ ] 相同 Idempotency-Key 返回同一任务；
- [ ] 不同输入 fingerprint 不错误复用任务；
- [ ] worker 崩溃后 heartbeat 可识别 interrupted；
- [ ] retry 不超过上限；
- [ ] cancel 后不继续消费下一阶段；
- [ ] Provider mock 证明 Job commit 发生在 remote call 之前；
- [ ] DB / logs / API response 不泄露 key；
- [ ] STALE 输入被 fail-closed 拒绝。

---

# P5 视频技术预处理

```text
Episode
→ media preflight
→ source timebase
→ Shot Boundary
→ thumbnail
→ Reference Clip
```

Shot Boundary 只是时间锚点，不是逐镜语义拉片。

输出：

- [ ] ShotAnchor；
- [ ] start_us / end_us；
- [ ] thumbnail；
- [ ] reference_clip；
- [ ] detector profile；
- [ ] revision / fingerprint。

验收：

- [ ] Shot 有序不重叠；
- [ ] Reference Clip 可完整 decode；
- [ ] 改边界后下游 Artifact STALE。

---

# P6 ASR / OCR / Source Dialogue

- [ ] ASR evidence；
- [ ] OCR / subtitle evidence；
- [ ] SourceDialogueUtterance；
- [ ] ShotDialogueProjection 1:N；
- [ ] canonical text provenance；
- [ ] raw evidence immutable。

硬规则：

```text
1 SourceDialogueUtterance
→ N ShotDialogueProjection
```

跨 Shot 的一句完整对白不能复制成多条业务对白。

---

# P7 整集理解 + Story / Rhythm

Professional Skills：

```text
source-video-understanding
story-rhythm-analysis
```

## SourceEpisodeUnderstandingProvider

输入：

- 完整 Episode；
- ASR / OCR compact evidence；
- ShotAnchor 时间范围。

输出：

- [ ] episode_summary；
- [ ] source_world；
- [ ] story_beats；
- [ ] character candidates；
- [ ] relationships；
- [ ] scenes；
- [ ] key props；
- [ ] key events；
- [ ] emotion timeline。

## StorySkeleton

- [ ] Hook；
- [ ] Conflict；
- [ ] Escalation；
- [ ] Reveal；
- [ ] Reversal；
- [ ] Emotional Peak；
- [ ] Payoff；
- [ ] Cliffhanger；
- [ ] beat timing。

## RhythmSkeleton

- [ ] shot order；
- [ ] shot duration baseline；
- [ ] key beat timing；
- [ ] reaction / dialogue rhythm；
- [ ] allowable deviation。

## Step 3.7 Flash 验收

当前只是重点候选：

- [ ] Agent reasoning 能力验收；
- [ ] 正式 API / Runtime 视频输入能力确认；
- [ ] 真实完整 Episode 故事理解 A/B；
- [ ] 结构化输出稳定性；
- [ ] 不覆盖 canonical source dialogue；
- [ ] 音频能力不做未经验证的假设；
- [ ] 延迟 / 成本 / 显存记录。

---

# P8 带 Source Bible 的逐镜精细拉片

Professional Skill：`shot-breakdown`。

每 Shot 至少：

- [ ] visual_description；
- [ ] visible_subjects；
- [ ] action；
- [ ] expression；
- [ ] posture；
- [ ] gaze；
- [ ] interaction；
- [ ] blocking；
- [ ] scene；
- [ ] props；
- [ ] shot_type；
- [ ] camera_angle；
- [ ] composition；
- [ ] camera_motion；
- [ ] lighting；
- [ ] continuity；
- [ ] narrative_function。

候选模型：Step 3.7 Flash / Qwen3.8 等做真实 A/B。

Source Bible 只提供上下文，当前画面不可见的事实不能伪造成可见事实。

---

# P9 Speaker / Character / Scene / Prop 归一

## Speaker

- [ ] audio-visual evidence；
- [ ] OFFSCREEN；
- [ ] UNKNOWN；
- [ ] 同 utterance 正式 speaker 唯一。

## Character

- [ ] face；
- [ ] body / ReID；
- [ ] clothing / appearance；
- [ ] temporal continuity；
- [ ] voice / context；
- [ ] Source Bible role；
- [ ] global identity graph。

禁止 Face / Track 直接成为 Character。

## Scene / Prop

- [ ] stable ID；
- [ ] dedup；
- [ ] shot binding；
- [ ] 只把剧情重要道具升级为全局 Prop。

---

# P10 SourceVideoSnapshot / 原片分析定稿

正式视频 Source Facts 边界至少包含：

- [ ] Episode；
- [ ] ShotAnchor；
- [ ] Reference Clip；
- [ ] Source Episode Bible；
- [ ] StorySkeleton；
- [ ] RhythmSkeleton；
- [ ] SourceDialogueUtterance；
- [ ] ShotDialogueProjection；
- [ ] SourceCharacter；
- [ ] SourceScene；
- [ ] SourceProp；
- [ ] SourceShotFact；
- [ ] Speaker Binding；
- [ ] revision / fingerprint。

用户页面称 **“原片分析定稿”**，不显示 `SourceVideoSnapshot` 技术名称或 raw evidence。

---

# P11 REPLICA Target Bible / 本土化替换

- [ ] AdaptationPlan；
- [ ] TargetWorld；
- [ ] TargetCharacter；
- [ ] TargetScene；
- [ ] TargetProp；
- [ ] TargetVoice；
- [ ] continuity rules；
- [ ] cultural localization decisions。

硬规则：故事骨架 / 节奏骨架默认锁定。

---

# P12 REPLICA Target Dialogue / TTS

```text
Source Dialogue
→ Translation
→ Localization
→ Final Target Dialogue
→ Voice
→ TTS
→ Actual Duration
```

- [ ] 台词自然；
- [ ] 意图 / 情绪保持；
- [ ] 真实 TTS duration 持久化；
- [ ] 优先改写短句适配原节奏；
- [ ] 禁止全局强制变速。

---

# P13 REPLICA Storyboard / Timing

每个 TargetStoryboardShot 至少：

- [ ] source_shot_ref；
- [ ] story_beat_ref；
- [ ] rhythm_constraint；
- [ ] target characters；
- [ ] scene；
- [ ] props；
- [ ] action；
- [ ] performance；
- [ ] framing / camera；
- [ ] dialogue；
- [ ] target_duration；
- [ ] timing_strategy。

用户必须能在视频生成前编辑业务分镜。

---

# P14 H3 Generation

```text
VideoGenerationProvider
→ MiniMaxH3Provider
→ local runtime
```

GenerationSegment：

- [ ] 与 StoryboardShot 独立；
- [ ] duration 合法；
- [ ] source reference video 去源音轨；
- [ ] target audio condition；
- [ ] continuity context；
- [ ] seed；
- [ ] fingerprint。

验收必须包含真实本地 Runtime + 真实 Segment。

---

# P15 H3 QC / Selection

- [ ] ffprobe；
- [ ] full decode；
- [ ] duration gate；
- [ ] character consistency；
- [ ] source actor leakage；
- [ ] scene；
- [ ] action；
- [ ] camera；
- [ ] continuity；
- [ ] finite retry；
- [ ] GenerationSelection。

只有 Selection 可以进入后期。

---

# P16 Post / Output

- [ ] Target audio；
- [ ] Lip Sync；
- [ ] safe background / SFX / music；
- [ ] subtitles；
- [ ] edit；
- [ ] EpisodeOutput；
- [ ] ProjectOutput；
- [ ] full decode；
- [ ] 用户看 / 听验收。

---

# P17 TRANSLATION

复用：

```text
视频输入
Source Dialogue
Speaker
Localization
TTS
Timing
Lip Sync
Post
```

默认不跑完整 Target Asset / H3 重绘链。

---

# P18 REDRAW

复用原片分析定稿 + Storyboard + H3 + QC。

重点增加：

- [ ] visual redesign Skill；
- [ ] style / actor / region replacement；
- [ ] KEEP_SOURCE_AUDIO / REGENERATE_AUDIO；
- [ ] source rhythm preservation。

---

# P19 SCRIPT_TO_DRAMA

- [ ] SourceTextSnapshot；
- [ ] script-analysis Skill；
- [ ] Target Bible；
- [ ] storyboard-directing；
- [ ] TTS / H3 / Post。

---

# P20 NOVEL_TO_DRAMA

- [ ] novel-adaptation Skill；
- [ ] chapter / event extraction；
- [ ] Episode outline；
- [ ] Hook / Conflict / Reversal / Cliffhanger；
- [ ] short-drama script；
- [ ] 接入共享生成链。

---

# P21 SCRIPT_LOCALIZATION

- [ ] script-localization Skill；
- [ ] localization intensity；
- [ ] cultural decision artifacts；
- [ ] Target Script；
- [ ] export；
- [ ] 一键派生 SCRIPT_TO_DRAMA。

---

# P22 Canvas View / 专业工作区

P22 才考虑接近 Seko 的可视化资产关系：

- [ ] Artifact Graph view；
- [ ] character / scene / prop / storyboard 节点；
- [ ] relation rendering；
- [ ] 资产复用；
- [ ] 局部重生成；
- [ ] 跨 Episode 上下文复用。

第一阶段禁止为了做画布而牺牲业务正确性。

---

# P23 多集与生产稳定

- [ ] Episode 串行队列；
- [ ] 中断恢复；
- [ ] checkpoint；
- [ ] GPU / VRAM 调度；
- [ ] Provider 限流；
- [ ] 磁盘管理；
- [ ] 失败诊断；
- [ ] 性能统计；
- [ ] 多集人物 / 世界连续性；
- [ ] 真实项目长期验收。

---

# 每阶段统一验收模板

每个阶段必须分别记录：

```text
A. 规划 / 契约
B. 数据模型 / API
C. 自动测试
D. CI
E. 前端真实交互
F. Provider / Runtime 真机
G. 真实素材
H. 业务输出质量
I. 用户最终验收
```

不允许：

```text
写了 Provider 类 = 模型跑通
pytest 通过 = 真实短剧质量通过
接口存在 = 功能完成
```

---

# 当前唯一下一步

```text
P4 Task / ProviderJob / Guardrail
```

P4 先把任务执行、幂等、恢复、重试、取消和 ProviderJob 先落稳；在 P4 完成以前，不接真实 ASR / OCR / Step 3.7 Flash / H3 Provider 请求。
