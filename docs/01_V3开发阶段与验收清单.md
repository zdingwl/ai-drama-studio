# AI Drama Studio V3 开发阶段与验收清单

> 版本：V3.1 Skill Architecture  
> 日期：2026-09-08  
> 依据：`00_V3产品与系统详细规划.md`。  
> 原则：一次完成一个可真实验收的纵向切片，不同时铺开六条半成品流程。

---

# P0 仓库重建基线 ✅

- [x] 清空旧 main；
- [x] 建立回退分支；
- [x] 写 V3 产品总纲；
- [x] 写开发规则；
- [x] 写阶段验收清单。

---

# P1 新工程骨架 ✅

## 后端

- [x] `backend/`；
- [x] FastAPI；
- [x] Pydantic Settings；
- [x] SQLAlchemy 2.x；
- [x] Alembic；
- [x] pytest；
- [x] 健康检查；
- [x] 统一错误响应；
- [x] UTC 时间策略；
- [x] Artifact 根目录。

## 前端

- [x] Vue 3；
- [x] TypeScript；
- [x] Pinia；
- [x] Vue Router；
- [x] Vite；
- [x] Vitest；
- [x] API client；
- [x] 基础布局。

## CI

- [x] backend install / compile / import / pytest；
- [x] frontend install / typecheck / test / build；
- [x] V3 CI 全绿。

---

# P2 Project + Skill Kernel ✅

P2 不再实现“六套固定 stage graph”。

P2 已建立：

```text
Project Type
→ Root Skill
→ Plan Compiler
→ ProjectExecutionPlan
→ Artifact Graph 基础
```

## P2.1 Project

- [x] `Project` model；
- [x] `ProjectType` 六枚举；
- [x] source_language；
- [x] target_language；
- [x] target_region；
- [x] scene_strategy；
- [x] audio_policy；
- [x] visual_style；
- [x] root_skill_id；
- [x] root_skill_version；
- [x] workflow_revision；
- [x] UTC timestamps；
- [x] Alembic migration。

六种类型：

```text
REPLICA
REDRAW
TRANSLATION
NOVEL_TO_DRAMA
SCRIPT_TO_DRAMA
SCRIPT_LOCALIZATION
```

V3 不兼容旧数据库，不提供 legacy 默认 project_type。

## P2.2 Skill Definition / Registry

- [x] `SkillDefinition` 数据契约；
- [x] `SkillManifest` schema；
- [x] `SkillRegistry`；
- [x] Skill version；
- [x] Skill category；
- [x] when_to_use / when_not_to_use；
- [x] required_inputs；
- [x] readable_artifacts；
- [x] required_capabilities；
- [x] subskills；
- [x] output_contracts；
- [x] completion_criteria；
- [x] failure_policy。

首版 Skill 正文与机器 manifest 保存在仓库：

```text
skills/projects/*/SKILL.md
skills/projects/*/manifest.json
```

机器执行读取结构化 manifest，不靠正则解析 Markdown。Registry 启动时会校验 schema、重复 Skill ID、重复 ProjectType 与缺失手册并 fail-fast。

## P2.3 六个 Root Project Skill

- [x] `replica`；
- [x] `redraw`；
- [x] `translation`；
- [x] `novel_to_drama`；
- [x] `script_to_drama`；
- [x] `script_localization`。

每个 Root Skill 已声明：

- 项目目标；
- 必需输入；
- 需要的 Professional Capability；
- 预计正式 Artifact；
- 什么情况下需要用户决策；
- 完成条件；
- 失败策略。

Replica Root Skill 明确把**故事骨架（Story Skeleton）与节奏骨架（Rhythm Skeleton）**作为目标版本权威约束。

## P2.4 Capability Registry

- [x] `CapabilityDefinition`；
- [x] `CapabilityRegistry`；
- [x] Skill 只依赖 capability；
- [x] capability 不绑定模型名；
- [x] capability 有 `PLANNED / AVAILABLE` 可用性，Provider 后续可以提供实现。

P2 只声明能力，不伪造真实实现。当前尚未进入后续阶段的 capability 保持 `PLANNED`，Plan 会显示“等待后续能力接入”。

## P2.5 Artifact Graph 最小内核

- [x] `Artifact`；
- [x] artifact_type；
- [x] revision；
- [x] input_fingerprint；
- [x] skill_id / skill_version；
- [x] current / stale；
- [x] `ArtifactRelation`；
- [x] relation type；
- [x] source / target / production namespace。

同时已实现：

- 同类型新 revision 产生后旧版本 STALE；
- 下游依赖沿 ArtifactRelation 自动 STALE；
- 关系不能引用不存在 Artifact；
- 关系不能跨 Project；
- Target / Production 不允许反向写入 Source；
- `CONTAINS` 与 `SUPERSEDES` 有 namespace / type 约束。

P2 不实现视频业务 Artifact 内容，只建立统一机制。

## P2.6 ProjectExecutionPlan

- [x] `ProjectExecutionPlan`；
- [x] `PlanStep`；
- [x] plan revision；
- [x] plan input fingerprint；
- [x] root skill version；
- [x] required capability；
- [x] dependencies；
- [x] expected outputs；
- [x] current plan pointer。

Plan 与 PlanStep 已真实持久化到数据库，不是 GET 时临时计算。

## P2.7 Plan Compiler

首版已实现确定性、可重复编译：

- [x] 根据 `project_type + root skill + 当前 artifact` 编译计划；
- [x] 同样输入得到同样 Plan / fingerprint，并幂等返回同一当前 Plan；
- [x] 不需要的能力不创建假完成步骤；
- [x] 未接入 capability 显示 `WAITING_CAPABILITY`；
- [x] 用户配置变化使旧 Plan 失效，必须显式 compile 新 Plan；
- [x] Artifact 变化使旧 Plan 失效；
- [x] GET 不自动 compile；
- [x] Plan 持久化后页面刷新不改变。

后续 AgentReasoningProvider 可以增强规划，但不能破坏上述确定性边界。

## P2.8 API

- [x] `GET /api/v3/skills`；
- [x] `GET /api/v3/skills/{id}`；
- [x] `GET /api/v3/skills/capabilities`；
- [x] `POST /api/v3/projects`；
- [x] `GET /api/v3/projects`；
- [x] `GET /api/v3/projects/{id}`；
- [x] `PATCH /api/v3/projects/{id}`；
- [x] `GET /api/v3/projects/{id}/plan`；
- [x] `POST /api/v3/projects/{id}/commands/compile-plan`；
- [x] `GET /api/v3/projects/{id}/artifacts`；
- [x] `GET /api/v3/projects/{id}/artifact-graph`。

## P2.9 前端

- [x] 六张“创建什么”项目类型卡片；
- [x] 不使用自由文本填写固定枚举；
- [x] 根据项目类型显示需要的基础字段；
- [x] 项目卡展示项目类型；
- [x] 工作区读取后端持久化 Plan；
- [x] 前端不维护独立 stage graph；
- [x] P2 用简单列表展示 Plan，不做无限画布；
- [x] 未实现 capability 直接显示“等待后续能力接入”。

## P2 必测

- [x] 六个 ProjectType 都能持久化；
- [x] 六个 Root Skill 都能加载；
- [x] ProjectType 与 Root Skill 映射正确；
- [x] 非法枚举 422；
- [x] Skill manifest schema 错误 fail-fast；
- [x] 同输入 Plan 稳定；
- [x] 不同 ProjectType Plan 不同；
- [x] GET 不写库；
- [x] GET 不执行 Agent / Provider；
- [x] Plan compile 是显式 command；
- [x] ArtifactRelation 不允许引用不存在 Artifact；
- [x] source / target namespace 不被混写。

## P2 真实 CI

```text
Run: 34195020700
Head: e594a9d3479152ef9dd2733770b3d8d3c662789f
Conclusion: success
```

通过：

```text
backend install
backend compileall
FastAPI import
Alembic 0001 → 0002 migration
pytest: 23 passed
frontend install
frontend typecheck
frontend unit test
frontend build
```

## P2 完成条件 ✅

用户可以创建六种项目，后端真正绑定对应版本化 Root Skill，并生成可持久化的当前执行计划。

**P2 完成不代表六种业务能力已实现。** 视频上传、文本上传、ASR/OCR、整集理解、逐镜拉片、人物识别、MiniMax H3 和口型同步均属于 P3 之后的真实能力阶段。

---

# P3 SourceAsset + 输入系统 ▶ 当前阶段

## 通用

- [ ] SourceAsset；
- [ ] filename / mime / size；
- [ ] SHA256；
- [ ] media/document type；
- [ ] 安全路径；
- [ ] immutable raw source；
- [ ] 上传幂等；
- [ ] Artifact Graph binding。

## 视频项目

适用：REPLICA / REDRAW / TRANSLATION。

- [ ] Episode；
- [ ] 多视频上传；
- [ ] 拖动排序；
- [ ] Episode order；
- [ ] ffprobe；
- [ ] decode preflight。

## 文本项目

- [ ] TXT；
- [ ] Markdown；
- [ ] SourceDocument；
- [ ] 编码检查；
- [ ] document revision；
- [ ] DOCX / PDF 留给 Document Extractor 后续扩展。

## 验收

- [ ] 批量 Episode 保持用户排序；
- [ ] 视频损坏明确失败；
- [ ] 文本编码异常明确失败；
- [ ] 路径穿越被拒绝；
- [ ] 替换源文件后相关 Artifact STALE；
- [ ] Plan 可据新 Artifact 状态重新显式编译。

---

# P4 Workflow / Task / ProviderJob

## Task

- [ ] explicit command create；
- [ ] Idempotency-Key；
- [ ] queue；
- [ ] worker；
- [ ] heartbeat；
- [ ] checkpoint；
- [ ] retry limit；
- [ ] cancel；
- [ ] interrupted resume；
- [ ] last_error。

## ProviderJob

- [ ] 外部 / 计费 Provider 请求前先持久化；
- [ ] capability；
- [ ] provider / model；
- [ ] payload fingerprint；
- [ ] status / attempt；
- [ ] safe error；
- [ ] 不保存 key。

## Guardrail

- [ ] GET read-only；
- [ ] Skill 无权绕过 ProviderJob；
- [ ] Skill 无权消费 STALE Artifact；
- [ ] Task 技术完成不等于业务 Artifact READY。

---

# P5 视频技术预处理

```text
Episode
→ media preflight
→ source timebase
→ Shot Boundary
→ thumbnail
→ Reference Clip
```

Shot Boundary 只是时间锚点，不是逐镜语义拉片。

输出：

- [ ] ShotAnchor；
- [ ] start_us / end_us；
- [ ] thumbnail；
- [ ] reference_clip；
- [ ] detector profile；
- [ ] revision / fingerprint。

验收：

- [ ] Shot 有序不重叠；
- [ ] Reference Clip 可完整 decode；
- [ ] 改边界后下游 Artifact STALE。

---

# P6 ASR / OCR / Source Dialogue

- [ ] ASR evidence；
- [ ] OCR / subtitle evidence；
- [ ] SourceDialogueUtterance；
- [ ] ShotDialogueProjection 1:N；
- [ ] canonical text provenance；
- [ ] raw evidence immutable。

硬规则：

```text
1 SourceDialogueUtterance
→ N ShotDialogueProjection
```

跨 Shot 的一句完整对白不能复制成多条业务对白。

---

# P7 整集理解 + Story / Rhythm

Professional Skills：

```text
source-video-understanding
story-rhythm-analysis
```

## SourceEpisodeUnderstandingProvider

输入：

- 完整 Episode；
- ASR / OCR compact evidence；
- ShotAnchor 时间范围。

输出：

- [ ] episode_summary；
- [ ] source_world；
- [ ] story_beats；
- [ ] characters candidates；
- [ ] relationships；
- [ ] scenes；
- [ ] key props；
- [ ] key events；
- [ ] emotion timeline。

## StorySkeleton

- [ ] Hook；
- [ ] Conflict；
- [ ] Escalation；
- [ ] Reveal；
- [ ] Reversal；
- [ ] Emotional Peak；
- [ ] Payoff；
- [ ] Cliffhanger；
- [ ] beat timing。

## RhythmSkeleton

- [ ] shot order；
- [ ] shot duration baseline；
- [ ] key beat timing；
- [ ] reaction/dialogue rhythm；
- [ ] allowable deviation。

## Step 3.7 Flash 验收

当前作为重点候选：

- [ ] Agent reasoning 能力验收；
- [ ] 完整 Episode 视频输入能力按正式 API / Runtime 验收；
- [ ] 真实短剧故事理解 A/B；
- [ ] 不覆盖 canonical source dialogue；
- [ ] 音频能力不做未经验证的假设。

---

# P8 带 Source Bible 的逐镜精细拉片

Professional Skill：`shot-breakdown`。

每 Shot 至少：

- [ ] visual_description；
- [ ] visible_subjects；
- [ ] action；
- [ ] expression；
- [ ] posture；
- [ ] gaze；
- [ ] interaction；
- [ ] blocking；
- [ ] scene；
- [ ] props；
- [ ] shot_type；
- [ ] camera_angle；
- [ ] composition；
- [ ] camera_motion；
- [ ] lighting；
- [ ] continuity；
- [ ] narrative_function。

候选模型：Step 3.7 Flash / Qwen3.8 等做真实 A/B。

Source Bible 只提供上下文，当前画面不可见的事实不能伪造成可见事实。

---

# P9 Speaker / Character / Scene / Prop 归一

## Speaker

- [ ] audio-visual evidence；
- [ ] OFFSCREEN；
- [ ] UNKNOWN；
- [ ] 同 utterance 正式 speaker 唯一。

## Character

- [ ] face；
- [ ] body / ReID；
- [ ] clothing / appearance；
- [ ] temporal continuity；
- [ ] voice/context；
- [ ] Source Bible role；
- [ ] global identity graph。

禁止 Face / Track 直接成为 Character。

## Scene / Prop

- [ ] stable ID；
- [ ] dedup；
- [ ] shot binding；
- [ ] 只把剧情重要道具升级为全局 Prop。

---

# P10 SourceVideoSnapshot

正式视频 Source Facts 边界。

至少：

- [ ] Episode；
- [ ] ShotAnchor；
- [ ] Reference Clip；
- [ ] Source Episode Bible；
- [ ] StorySkeleton；
- [ ] RhythmSkeleton；
- [ ] SourceDialogueUtterance；
- [ ] ShotDialogueProjection；
- [ ] SourceCharacter；
- [ ] SourceScene；
- [ ] SourceProp；
- [ ] SourceShotFact；
- [ ] Speaker Binding；
- [ ] revision / fingerprint。

用户页面直接显示业务结果，不显示 raw evidence。

---

# P11 REPLICA Target Bible

- [ ] AdaptationPlan；
- [ ] TargetWorld；
- [ ] TargetCharacter；
- [ ] TargetScene；
- [ ] TargetProp；
- [ ] TargetVoice；
- [ ] continuity rules；
- [ ] cultural localization decisions。

硬规则：Story / Rhythm 默认锁定。

---

# P12 REPLICA Target Dialogue / TTS

```text
Source Dialogue
→ Translation
→ Localization
→ Final Target Dialogue
→ Voice
→ TTS
→ Actual Duration
```

- [ ] 台词自然；
- [ ] 意图 / 情绪保持；
- [ ] 真实 TTS duration 持久化；
- [ ] 优先改写短句适配节奏；
- [ ] 禁止全局强制变速。

---

# P13 REPLICA Storyboard / Timing

每个 TargetStoryboardShot 至少：

- [ ] source_shot_ref；
- [ ] story_beat_ref；
- [ ] rhythm_constraint；
- [ ] target characters；
- [ ] scene；
- [ ] props；
- [ ] action；
- [ ] performance；
- [ ] framing / camera；
- [ ] dialogue；
- [ ] target_duration；
- [ ] timing_strategy。

用户必须能在视频生成前编辑业务分镜。

---

# P14 H3 Generation

```text
VideoGenerationProvider
→ MiniMaxH3Provider
→ local runtime
```

GenerationSegment：

- [ ] 与 StoryboardShot 独立；
- [ ] duration 合法；
- [ ] source reference video 去源音轨；
- [ ] target audio condition；
- [ ] continuity context；
- [ ] seed；
- [ ] fingerprint。

验收必须包含真实本地 Runtime + 真实 Segment。

---

# P15 H3 QC / Selection

- [ ] ffprobe；
- [ ] full decode；
- [ ] duration gate；
- [ ] character consistency；
- [ ] source actor leakage；
- [ ] scene；
- [ ] action；
- [ ] camera；
- [ ] continuity；
- [ ] finite retry；
- [ ] GenerationSelection。

只有 Selection 可以进入后期。

---

# P16 Post / Output

- [ ] Target audio；
- [ ] Lip Sync；
- [ ] safe background / SFX / music；
- [ ] subtitles；
- [ ] edit；
- [ ] EpisodeOutput；
- [ ] ProjectOutput；
- [ ] full decode；
- [ ] 用户看 / 听验收。

---

# P17 TRANSLATION

复用：

```text
视频输入
Source Dialogue
Speaker
Localization
TTS
Timing
Lip Sync
Post
```

默认不跑完整 Target Asset / H3 重绘链。

---

# P18 REDRAW

复用 SourceVideoSnapshot + Storyboard + H3 + QC。

重点增加：

- [ ] visual redesign Skill；
- [ ] style / actor / region replacement；
- [ ] KEEP_SOURCE_AUDIO / REGENERATE_AUDIO；
- [ ] source rhythm preservation。

---

# P19 SCRIPT_TO_DRAMA

- [ ] SourceTextSnapshot；
- [ ] script-analysis Skill；
- [ ] Target Bible；
- [ ] storyboard-directing；
- [ ] TTS / H3 / Post。

---

# P20 NOVEL_TO_DRAMA

- [ ] novel-adaptation Skill；
- [ ] chapter / event extraction；
- [ ] Episode outline；
- [ ] Hook / Conflict / Reversal / Cliffhanger；
- [ ] short-drama script；
- [ ] 接入共享生成链。

---

# P21 SCRIPT_LOCALIZATION

- [ ] script-localization Skill；
- [ ] localization intensity；
- [ ] cultural decision artifacts；
- [ ] Target Script；
- [ ] export；
- [ ] 一键派生 SCRIPT_TO_DRAMA。

---

# P22 Canvas View / 专业工作区

P22 才考虑接近 Seko 的可视化资产关系。

- [ ] Artifact Graph view；
- [ ] character / scene / prop / storyboard 节点；
- [ ] relation rendering；
- [ ] 资产复用；
- [ ] 局部重生成；
- [ ] 跨 Episode 上下文复用。

第一阶段禁止为了做画布而牺牲业务正确性。

---

# P23 多集与生产稳定

- [ ] Episode 串行队列；
- [ ] 中断恢复；
- [ ] checkpoint；
- [ ] GPU / VRAM 调度；
- [ ] Provider 限流；
- [ ] 磁盘管理；
- [ ] 失败诊断；
- [ ] 性能统计；
- [ ] 多集人物 / 世界连续性；
- [ ] 真实项目长期验收。

---

# 每阶段统一验收模板

每个阶段必须分别记录：

```text
A. 规划 / 契约
B. 数据模型 / API
C. 自动测试
D. CI
E. 前端真实交互
F. Provider / Runtime 真机
G. 真实素材
H. 业务输出质量
I. 用户最终验收
```

不允许：

```text
写了 Provider 类 = 模型跑通
pytest 通过 = 真实短剧质量通过
接口存在 = 功能完成
```

---

# 当前唯一下一步

```text
P3 SourceAsset + 输入系统
```

P3 先完成真实文件输入、Episode / SourceDocument、SHA256、安全路径、排序、预检与 Artifact Graph 绑定；在 P3 完成以前，不进入 ASR / OCR / 整集理解 / 逐镜拉片 / H3。