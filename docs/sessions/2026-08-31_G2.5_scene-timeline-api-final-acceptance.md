# Session Handoff — G2.5 Scene Timeline API Final Acceptance

Date: 2026-08-31

Repository: `zdingwl/ai-drama-studio`

Branch: `main`

## Final status

```text
G1 Fast Grounded = REAL ACCEPTED / PRODUCTION / FROZEN
G2.1 Scene Timeline Contract = FINAL PASS / FROZEN FOUNDATION
G2.2 Deterministic Assembler = FINAL PASS / FROZEN FOUNDATION
G2.3 Scene Narrative = FINAL PASS / FROZEN
G2.4 Source/Support Validator = FINAL PASS / FROZEN
Local Qwen text runtime = REAL ACCEPTED / FROZEN BASELINE
G2.5 Scene Timeline API = FINAL PASS / FROZEN
G2.6 ordinary-user Scene Timeline UI = NEXT
Character V10.1 = PROTECTED / UNCHANGED
```

## Accepted production Run

```text
Run = BREAKDOWNRUN_6953039fc8a940b6b239f6475cd537e4
Episode = EPISODE_0ed6aaca0da4471db0364bd29c3d6a61
Shots = 30
Scenes = 2
```

Accepted Narrative remains:

```text
Scene 1 title = 走廊争花
Scene 1 summary = 老年女性质问年轻女性为何将花放在自家花瓶，年轻女性称花在走廊，双方争执并最终以给钱解决，年轻女性愤怒指责对方。

Scene 2 title = 客厅争执
Scene 2 summary = 人物2指责人物1对邻居偷花一事不作为，称其结婚八年从未支持过自己，人物1则表示自己会自行解决。
```

## User-local Windows/CUDA acceptance evidence

Narrative materialization:

```powershell
python scripts/materialize_breakdown_g2_scene_timeline_v1.py BREAKDOWNRUN_6953039fc8a940b6b239f6475cd537e4 --device cuda
```

Observed:

```json
{
  "run_id": "BREAKDOWNRUN_6953039fc8a940b6b239f6475cd537e4",
  "scene_count": 2,
  "accepted_title_count": 2,
  "accepted_summary_count": 2,
  "warning_count": 0,
  "runtime_profile": "breakdown-g2-scene-narrative-qwen3-local-v1"
}
```

Artifact was written under the accepted Run workspace as:

```text
scene-timeline/narrative-overlay-v1.json
```

New G2.5 tests:

```powershell
python -m pytest engine/tests/v2/test_breakdown_scene_timeline_result_v1.py engine/tests/v2/test_breakdown_scene_timeline_routes_v1.py -q
```

Observed:

```text
12 passed
```

Frozen G2 regression:

```powershell
python -m pytest engine/tests/v2/test_breakdown_scene_timeline_v1.py engine/tests/v2/test_breakdown_scene_narrative_v1.py engine/tests/v2/test_breakdown_scene_narrative_qwen3_v1.py engine/tests/v2/test_breakdown_scene_narrative_real_regression_v1.py -q
```

Observed terminal output contained 19 passing test dots and no failure/error output.

## Frozen G2.5 API boundary

```text
GET /api/episodes/{episode_id}/scene-timeline
GET /api/breakdown-runs/{run_id}/scene-timeline
```

Rules now frozen:

```text
GET never starts Qwen/model inference.
Narrative inference/materialization is explicit.
Missing/stale/invalid Narrative falls back to deterministic G2.2 Scene Timeline.
Persisted Narrative must replay through frozen G2.4 validation before use.
Only title/story_summary may be overlaid.
Primary API must not expose support Fxxxx, source_fingerprint, Evidence IDs, cluster keys, LocalSubject DB IDs, confidence/provider/model diagnostics, raw validator diagnostics, or Final Asset IDs.
Character V10.1 remains untouched.
```

## Next stage — G2.6

Build the ordinary-user Scene Timeline UI on top of the frozen G2.5 read API.

Target reading order:

```text
Scene title + story summary
→ Scene info
→ people
→ Shot cards
   → thumbnail / time
   → visual description
   → people/performance/action
   → dialogue
   → props
   → cinematography
   → OCR/on-screen text
```

Ordinary users should see direct results, not engineering evidence. Do not surface support Fxxxx, Evidence IDs, LocalSubject IDs, cluster keys, confidence/provider/model diagnostics, or raw validation traces.

Do not reopen G1/G2.1-G2.5/Character V10.1 without a concrete regression.

No assistant-local pytest/CUDA PASS is claimed. The supplied user-local Windows/CUDA terminal output is the acceptance truth.
