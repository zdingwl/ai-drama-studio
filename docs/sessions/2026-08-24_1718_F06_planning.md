# Session — F06 自动人物识别规划

时间：2026-08-24 17:18 +08:00  
Feature：F06 自动人物识别  
状态：PLANNED / CONTRACT CONFIRMED  
代码：NOT STARTED

## 用户确认

用户确认开始规划 F06，并同意将“人物对白”大工作区拆回已经冻结的 35 Feature 正式顺序：

```text
F06 自动人物识别
F07 人物人工修正
F08 ASR 源对白识别
F09 Speaker / Character 匹配
F10 源对白人工修正
```

因此 F06 不包含 Whisper / Speaker / 人工对白。

## 已确认技术路线

```text
F05 confirmed Final Shots
→ 约 4 FPS 自适应采样，每 Shot 3–12 帧
→ OpenCV YuNet
→ OpenCV SFace
→ Shot-local Face Track
→ 保守跨 Shot Clustering
→ Character Candidate
```

关键原则：

```text
AI Candidate != Final Character
Precision > Recall
宁可拆错，不要合错
同框独立人物 Track 形成 cannot-link
```

## 计划依赖

```text
opencv-python==4.11.0.86
numpy==2.1.3（复用）
```

模型：

```text
face_detection_yunet_2023mar.onnx
face_recognition_sface_2021dec.onnx
```

V1 固定 OpenCV CPU DNN，不修改 F04 已冻结 PyTorch/CUDA 环境。

编码前必须计算实际模型 SHA-256 并写入 `config/models.yaml`。

## 已创建规划文档

```text
docs/features/F06-auto-character-detection.md
docs/features/F06-function-contracts.md
docs/features/F06-database-dictionary.md
docs/features/F06-p0-checklist.md
```

`docs/PROJECT_STATE.md` 已更新为：

```text
F01-F05 STABLE / FROZEN
F06 PLANNED / CONTRACT CONFIRMED
Coding NOT STARTED
```

## F06 DB Plan

Migration：

```text
0007_create_character_detection
```

只新增：

```text
character_detection_runs
character_candidates
character_tracks
```

## 对外函数 Plan

只保留 3 个公开业务函数：

```text
get_character_detection()
run_character_detection()
rerun_character_detection()
```

Detection / embedding / tracking / clustering 全部是内部函数，不各自创建 Controller。

## Coding Gate

下一步不是直接写业务算法，而是先完成环境 Gate：

```text
1. requirements 锁定 opencv-python==4.11.0.86
2. 固定 YuNet / SFace 文件
3. 计算 SHA-256 并更新 config/models.yaml
4. Windows 本机 smoke test FaceDetectorYN / FaceRecognizerSF
5. 通过后正式进入 F06 IN_PROGRESS
```

未完成以上环境 Gate 前，不声称 F06 已进入业务开发完成状态。
