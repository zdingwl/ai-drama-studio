# AI Drama Studio V3 当前开发状态

> 更新时间：2026-09-08

## 当前阶段

```text
P0 仓库重建基线        ✅ 完成
P1 新工程骨架          ✅ 完成
Seko Skill 架构研究    ✅ 完成第一版
P2 Project + Skill Kernel  ▶ 下一阶段
```

---

# P1 已完成

## 后端

- [x] `backend/` 全新初始化；
- [x] FastAPI app factory；
- [x] Pydantic Settings；
- [x] SQLAlchemy 2.x 基础设施；
- [x] Alembic 基础目录与配置；
- [x] pytest；
- [x] `/api/v3/health`；
- [x] App / Validation / HTTP 统一错误 envelope；
- [x] UTC aware datetime 策略；
- [x] Artifact 根目录配置与自动创建。

## 前端

- [x] `frontend/` 全新初始化；
- [x] Vue 3；
- [x] TypeScript；
- [x] Pinia；
- [x] Vue Router；
- [x] Vite；
- [x] Vitest；
- [x] 统一 API client 基础；
- [x] 基础工作台布局。

## CI

最近确认的 P1 完整 CI：

```text
Run: 34188576809
Head: f4745b9d65549c5f412a9066794b2d13af32af96
Conclusion: success
```

真实通过：

- [x] backend dependency install；
- [x] backend compileall；
- [x] FastAPI import；
- [x] backend pytest；
- [x] frontend dependency install；
- [x] frontend typecheck；
- [x] frontend unit test；
- [x] frontend build。

---

# Seko 3.0 Skill 架构研究

已完成第一版公开资料逆向分析：

```text
docs/03_Seko3.0_Skill架构逆向分析.md
```

已确认并纳入 V3 的核心思想：

```text
Agent
+
Skill
+
Artifact / 项目上下文
+
Provider / Tools
```

正式产品规划已从：

```text
project_type
→ 六套固定 stage graph
```

调整为：

```text
project_type
→ Root Project Skill
→ Agent / Plan Compiler
→ ProjectExecutionPlan
→ Professional Skills
→ Provider / Tools
→ Typed Artifacts
→ Artifact Graph
```

注意：没有获得、也没有声称获得 Seko 私有 System Prompt / 原始内部 Skill 文件。当前研究来自官方公开资料和公开实际使用行为。

---

# P2 当前正式范围

P2 已重新定义为：

```text
Project + Skill Kernel
```

需要实现：

- Project / ProjectType；
- 六个 Root Skill；
- Skill Manifest / Registry；
- Capability Registry；
- Artifact / ArtifactRelation 最小内核；
- ProjectExecutionPlan / PlanStep；
- 确定性 Plan Compiler；
- 六类项目创建入口；
- 后端 Plan 驱动前端工作区。

P2 暂不实现：

- 视频上传；
- ASR / OCR；
- Step 3.7 Flash 实际调用；
- Qwen3.8；
- 人物识别；
- 拉片；
- H3；
- 无限画布。

这些按 P3 以后逐阶段进入。

---

# Step 3.7 Flash 当前定位

当前只作为重点候选，不写成已验收生产模型。

规划角色：

```text
AgentReasoningProvider 候选
+
SourceEpisodeUnderstandingProvider 候选
+
SourceShotUnderstandingProvider A/B 候选
```

必须后续分别做：

- API / 本地 Runtime 能力确认；
- 真实完整 Episode；
- 真实逐镜 A/B；
- 结构化输出；
- 视频输入；
- 音频能力边界；
- 延迟 / 成本 / 显存；
- 故事 / 镜头准确率验收。

---

# 验收边界

当前已经完成的是：

```text
P1 工程地基
+
V3.1 Skill 架构规划
```

还没有完成：

```text
P2 Skill Kernel 代码
六类项目真实持久化
任何视频理解模型真实调用
Replica 业务链
MiniMax H3 生产链
```

不能把“规划完成”写成“功能完成”。