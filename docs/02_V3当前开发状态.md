# AI Drama Studio V3 当前开发状态

> 更新时间：2026-09-09  
> 当前产品编排基线：Source Understanding Orchestration  
> P7 最终验收：`docs/07_P7最终验收与ProviderReadiness.md`  
> P8 契约：`docs/08_P8逐镜精细拉片ProfessionalSkill与数据契约.md`  
> 当前整改与恢复验收：`docs/09_P6CanonicalEvidenceV2与P8最终验收整改.md`

---

# 当前阶段

```text
P0 仓库重建基线            ✅ 完成
P1 新工程骨架              ✅ 完成
P2 Project + Skill Kernel  ✅ 完成
P3 SourceAsset + 输入系统  ✅ 完成
P4 Task / ProviderJob      ✅ 完成
P5 镜头技术锚点            ✅ 完成（真实 Episode 人工验收通过）
P6 Source Evidence         ✅ 能力 AVAILABLE；v2 真实恢复与分段整改通过，仍有少量 ASR 质量项待最终音画确认
P7 整集多模态原片理解      ✅ 能力 AVAILABLE；已基于 P6 v2 恢复新 CURRENT rev4
P8 逐镜精细拉片            🔁 工程/真实数据库/真实 Provider 验收通过；仅剩最终 28 镜音画人工验收
P9 身份/场景/道具归一       ⛔ 未开始，当前禁止进入
```

当前已 `AVAILABLE` 的原片理解能力：

```text
SOURCE_VIDEO_INGEST
MEDIA_PREFLIGHT
SHOT_BOUNDARY
SOURCE_DIALOGUE_EVIDENCE
EPISODE_UNDERSTANDING
STORY_RHYTHM
```

仍为 `PLANNED`：

```text
SHOT_BREAKDOWN
IDENTITY_RESOLUTION
SCENE_RESOLUTION
PROP_RESOLUTION
SOURCE_SNAPSHOT
```

`SOURCE_DIALOGUE_EVIDENCE = AVAILABLE` 表示 P6 能力本身已经完成真实工程验收。P8 最终人工验收曾发现旧 `p6-source-evidence-v1` 的 canonical merge 过度激进；v2 已完成真实恢复链并消除多人超长串句根因，但最终音画签字前不把 P8 capability 提前升级。

---

# 当前 Source Understanding 架构

完整 Episode 永远是 Source Truth：

```text
SOURCE_VIDEO / 完整 Episode
= Source Truth
```

视频类项目内部编排：

```text
完整 Episode
        │
        ↓
Media Preflight
        │
        ├────────────────┬────────────────┐
        ↓                ↓                ↓
Shot Anchors         ASR Evidence      OCR Evidence
        │                │                │
        └────────────┬───┴────────────────┘
                     ↓
              Source Evidence / P6
                     +
               完整 Episode
                     ↓
          整集多模态原片理解 / P7
                     ↓
      SOURCE_BIBLE / Story / Rhythm
                     +
             Shot Anchors
                     ↓
          逐镜精细拉片 / P8
                     ↓
          SOURCE_SHOT_FACTS
```

硬规则：

- Shot Boundary 读取完整 `SOURCE_VIDEO`；
- P6 ASR 读取完整连续音轨，不能按 Shot / Reference Clip 分开识别；
- P6 OCR 扫描完整视频时间轴，Shot Anchors 只能辅助抽帧 / 去重；
- P6 Source Evidence 与 P7 Source Understanding 严格分离；
- P7 必须读取完整 Episode，不允许把每个 Shot 独立丢给模型猜剧情后再拼接；
- P7 对白 / OCR 的文字事实必须引用 CURRENT P6 Evidence；模型不得覆盖 canonical Source Evidence；
- P8 必须读取完整 Episode + CURRENT SOURCE_BIBLE + CURRENT SHOT_ANCHORS + CURRENT P6 canonical Evidence；
- P8 Shot 时间只认 P5；对白正文只认 P6；人物 / 场景 / 道具只能绑定 P7 candidate；
- P8 不得为了修正 ASR 质量自行重新听写。

---

# P6 当前基线：Canonical Evidence v2

旧 P6 v1 的真实样例曾出现：

```text
50 raw ASR segments
→ 激进相邻合并
→ 8 canonical utterances
```

最终 P8 人工检查确认其中存在多轮不同说话回合被串为一个 canonical utterance 的情况，导致 P8 虽然严格遵守 P6 正文，却把同一超长正文 overlap 到多个 Shot。

当前正式基线：

```text
p6-source-evidence-v2
segment-preserving-dialogue-v2
large-v3-turbo
```

策略：

- 完整 Episode 连续 ASR 架构不变；
- raw ASR segment 默认保持 canonical 边界；
- 仅有明确 continuation 标点、短时间 gap、语言兼容且总时长受限时允许合并；
- 不再仅凭“相邻 segment 很近”跨段合并；
- 不使用真实样例专用硬编码纠错词表；
- Provider profile / canonical policy 进入 fingerprint / provenance；
- 显式 P6 rerun 在已有正式 Evidence revision 后必须创建新 Task / revision，同轮重复提交仍去重。

迁移：

```text
0012_p6_canonical_evidence_v2
```

真实恢复链已验证旧链正确失效且历史保留：

```text
SOURCE_VIDEO rev1          CURRENT
SHOT_ANCHORS rev1          CURRENT
SOURCE_DIALOGUE rev1       STALE
SOURCE_BIBLE rev1–3        STALE
STORY_SKELETON rev1–3      STALE
RHYTHM_SKELETON rev1–3     STALE
SOURCE_SHOT_FACTS rev1     STALE
```

新 CURRENT：

```text
SOURCE_DIALOGUE rev2
SOURCE_BIBLE rev4
STORY_SKELETON rev4
RHYTHM_SKELETON rev4
SOURCE_SHOT_FACTS rev2
```

真实 P6 v2：

```text
56 raw ASR
→ 56 canonical dialogue
73 canonical OCR
```

多人超长串句根因已消失。当前仍有两处明显 ASR 错词与片尾两条极短重复 ASR，需要最终音画人工验收确认；详见 `docs/09`。

---

# P7 已完成能力基线

正式契约：

```text
source-video-understanding@1.1.0
p7-source-bible-v2
SOURCE_BIBLE schema 1.1
grounded-source-truth-v2
```

真实 Provider：

```text
Volcengine Ark
Doubao Seed 2.1 Pro
```

真实短剧人工验收已经确认 P7 的故事概述、人物、关系、场景、关键道具、时间化剧情、Story / Rhythm、Grounding 与 provenance 可用，因此：

```text
EPISODE_UNDERSTANDING = AVAILABLE
STORY_RHYTHM = AVAILABLE
```

P6 v2 恢复链中，P7 已重新运行并发布：

```text
SOURCE_BIBLE rev4      CURRENT
STORY_SKELETON rev4    CURRENT
RHYTHM_SKELETON rev4   CURRENT
```

provenance 正确引用新的 `SOURCE_DIALOGUE rev2` / `SourceEvidenceSet rev2`，没有恢复旧 Artifact。

---

# P8 当前实现与验收状态

正式 Professional Skill：

```text
shot-breakdown@1.0.0
```

正式输出：

```text
SOURCE_SHOT_FACTS schema 1.0
source-bible-shot-facts-v1
```

P8 已实现：

- 完整 Episode Provider 输入；
- P5 Shot Anchor authoritative timing；
- P6 canonical dialogue / OCR Shot binding；
- P7 character / scene / prop candidate binding；
- Provider 无 Shot time / dialogue text 正式输出通道；
- unknown P7 candidate fail closed；
- ProviderJob-before-call；
- revision / fingerprint / provenance / SUPERSEDES；
- CURRENT / STALE 传播；
- 显式 POST + 只读 GET；
- Seko 形态逐镜分镜表；
- Reference Clip 只做局部人工核对。

P6 v2 恢复后再次真实运行 P8：

```text
SOURCE_SHOT_FACTS rev2 CURRENT
旧 rev1                STALE
```

真实恢复验收结果：

- P5 28/28 Shot 时间与 P8 完全一致；
- 28/28 dialogue overlap 集合一致；
- P6 v2 canonical text 在 P8 中逐字绑定；
- OCR evidence ID 全部属于 CURRENT P6；
- P7 candidate 无越界；
- provenance / Artifact Graph / ProviderJob / fingerprint / SUPERSEDES 一致；
- 引用 STALE 上游 = 0；
- orphan revision / relation = 0；
- 同类型多个 CURRENT = 0；
- backend 104 tests PASS；
- frontend 11 files / 38 tests、typecheck、build PASS。

因此当前判定：

```text
P8 工程实现                  PASS
P8 自动测试                  PASS
P8 真实数据库一致性           PASS
P8 真实 Provider 执行         PASS
P8 P6 v2 恢复链               PASS
P8 最终逐镜音画人工验收        PENDING

SHOT_BREAKDOWN = PLANNED
```

---

# 当前真实验收样例

```text
项目类型：REPLICA
project_id：99f7036f-cce8-4700-a717-683278107866
Episode：货到付款惩治隔壁大妈-第01集.mp4
时长：约 66.36 秒
分辨率：1080 × 1920
P5：28 Shot
P6 v2：56 raw ASR / 56 canonical dialogue / 73 canonical OCR span
P7：SOURCE_BIBLE rev4 CURRENT
P8：SOURCE_SHOT_FACTS rev2 CURRENT
```

当前已没有旧 v1 的多人超长 canonical merge。最终人工验收需要重点确认两处残余 ASR 错词和片尾极短重复 ASR 是否影响后续制作可用性。

---

# 当前下一步

只允许执行：

```text
1. 在当前 SOURCE_SHOT_FACTS rev2 上完整播放 28 个 Reference Clip / 对应原片区间
2. 逐镜人工核对画面描述、镜头语言、人物/场景/道具绑定、对白 delivery、音效/环境声
3. 重点核对 P6 残余质量项：
   - “收店人” vs 实际语音 / OCR“收件人”
   - “我拿拎包入住的坠绪” vs 实际语音 / OCR“我那拎包入住的赘婿”
   - 66.020–66.220 两条极短重复“你还真去报警啊”是否为有效对白
4. 若 28 Shot 全部可用于后续制作，则记录最终人工验收 PASS
5. 只有最终人工验收 PASS 后，才允许评估 `SHOT_BREAKDOWN = AVAILABLE` 与 Issue #24 收尾
```

验收通过前：

```text
SHOT_BREAKDOWN = PLANNED
禁止进入 P9
```
