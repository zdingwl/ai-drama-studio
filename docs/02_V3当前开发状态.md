# AI Drama Studio V3 当前开发状态

> 更新时间：2026-09-11  
> 当前最高优先级：`docs/31_P13最终验收与P14准入评估.md`  
> 当前事实源：仓库当前 `main` + 编号更高、日期更新的状态 / 验收文档；历史文档只解释演进，不覆盖最新口径。  
> P10 最终验收与 P11 准入：`docs/23_P10最终验收与P11准入.md`  
> P11 最终验收与 P12 准入：`docs/26_P11最终验收与P12准入.md`  
> P12 最终验收：`docs/27_P12最终验收与后续阶段准入评估.md`  
> P13 正式合同：`docs/28_P13TargetAssetsProfessionalSkill与数据契约.md`  
> P13 中文审核语言：`docs/29_P13中文审核语言与生成执行语言分层.md`  
> P13 v3 整改：`docs/30_P13真实项目验收_资产作用域与时序约束整改.md`  
> P13 最终验收与 P14 准入边界：`docs/31_P13最终验收与P14准入评估.md`

---

# 1. 当前阶段

```text
P0 仓库重建基线                         ✅ 完成
P1 新工程骨架                           ✅ 完成
P2 Project + Skill Kernel               ✅ 完成
P3 SourceAsset + 输入系统               ✅ 完成
P4 Task / ProviderJob                   ✅ 完成
P5 Shot Anchors                         ✅ 完成
P6 Source Evidence                      ✅ AVAILABLE / 已验收
P7 整集多模态原片理解                   ✅ AVAILABLE / 已验收
P8 逐镜精细拉片                         ✅ AVAILABLE / 已验收
P9 Character/Speaker/Scene/Prop 最终归一 ✅ AVAILABLE / 已验收
P10 SourceVideoSnapshot / 一键原片解析   ✅ SOURCE_SNAPSHOT = AVAILABLE / P10 PASS
P11 Replica Target Bible                ✅ LOCALIZATION / TARGET_BIBLE = AVAILABLE / P11 PASS
P12 Target Script / Localization        ✅ TARGET_SCRIPT = AVAILABLE / P12 PASS
P13 Target Assets                       ✅ TARGET_ASSETS = AVAILABLE / P13 PASS
```

P13 当前正式状态：

```text
P13 real Provider path = EXECUTED
P13 v3 real project review = PASS
TARGET_ASSETS = AVAILABLE
P13 PASS = YES
P14+ = PLANNED / NOT STARTED
```

用户已于 2026-09-11 对 P13 v3 / `replica-target-visual-identity-v2` 真实复验明确确认 `P13 PASS`。最终状态以 `docs/31` 为准。旧 visual-identity-v1 的失败验收与整改原因仍保留在 `docs/30` 作为历史证据，不再代表当前阶段状态。

---

# 2. 当前能力状态

当前 `AVAILABLE`：

```text
SOURCE_VIDEO_INGEST
SOURCE_TEXT_INGEST
MEDIA_PREFLIGHT
SHOT_BOUNDARY
SOURCE_DIALOGUE_EVIDENCE
EPISODE_UNDERSTANDING
STORY_RHYTHM
SHOT_BREAKDOWN
IDENTITY_RESOLUTION
SCENE_RESOLUTION
PROP_RESOLUTION
SOURCE_SNAPSHOT
LOCALIZATION
TARGET_BIBLE
TARGET_SCRIPT
TARGET_ASSETS
```

当前仍为 `PLANNED`：

```text
SCRIPT_ANALYSIS
NOVEL_ADAPTATION
TTS
TIMING
STORYBOARD
VIDEO_GENERATION
QC_SELECTION
LIP_SYNC
POST_PRODUCTION
EXPORT_SCRIPT
```

`TARGET_ASSETS = AVAILABLE` 只表示 P13 自身完成真实人工验收，不自动使后续 Production / Generation 能力可用。

---

# 3. 当前 Source → Target 正式链路

完整 Episode 仍然是 Source Truth。当前已验收正式链路：

```text
SOURCE_VIDEO / 完整 Episode
        │
        ↓
P5 Shot Anchors
        +
P6 ASR / OCR Canonical Evidence
        +
P7 Episode Understanding / Source Bible / Story / Rhythm
        +
P8 Source Shot Facts
        +
P9 Character / Speaker / Scene / Prop Resolution
        │
        ↓
P10 CURRENT SOURCE_VIDEO_SNAPSHOT
        │
        ↓
P11 ADAPTATION_PLAN + TARGET_BIBLE
        │
        ├──────────────────────────────┐
        ↓                              ↓
P12 TARGET_SCRIPT                P13 TARGET_ASSETS
（AVAILABLE）                   （AVAILABLE）
```

硬边界继续保持：

- P5~P10 Source Facts 不允许被 Target / Production 阶段反向改写；
- P10 `SOURCE_VIDEO_SNAPSHOT` 是后续 Target 世界读取 Source Facts 的唯一当前版本锚点；
- P11 `TARGET_BIBLE` 定义正式 Target Character / Scene / Prop identity 与本土化语义；
- P12 只负责到 Final Target Dialogue，不负责资产、TTS、Timing、Storyboard 或 Generation；
- P13 唯一硬输入只有 CURRENT `TARGET_BIBLE`，只负责 Target Character / Scene / Prop 的视觉身份实现；
- `TARGET_SCRIPT` 不是 P13 输入，Target Script revision 变化不得无依据地使 TARGET_ASSETS STALE；
- P13 不负责 Target Voice、TTS、Timing、Storyboard、Generation、QC、Lip Sync 或 Post。

---

# 4. P10 / P11 / P12 / P13 已验收基线

## P10 ✅

```text
P10 PASS
SOURCE_SNAPSHOT = AVAILABLE
```

P10 将已验收 Source Facts 确定性冻结为 `CURRENT SOURCE_VIDEO_SNAPSHOT`，不重新调用 Provider 理解原片，不静默改写 P5/P6/P7/P8/P9 历史事实。最终状态以 `docs/23` 为准。

## P11 ✅

```text
P11 PASS
LOCALIZATION = AVAILABLE
TARGET_BIBLE = AVAILABLE
```

P11 正式硬输入仍只有 CURRENT `SOURCE_VIDEO_SNAPSHOT`，正式输出仍只有 `ADAPTATION_PLAN + TARGET_BIBLE`。最终状态以 `docs/26` 为准，技术合同以 `docs/24` 为准。

## P12 ✅

```text
P12 PASS
TARGET_SCRIPT = AVAILABLE
```

P12 正式硬输入固定为 CURRENT `SOURCE_VIDEO_SNAPSHOT + ADAPTATION_PLAN + TARGET_BIBLE`。Source Dialogue 只允许来自 Snapshot 冻结的 canonical dialogue；Provider 无权重新 ASR / OCR / 猜词或改变 canonical timing / utterance 集合。最终状态以 `docs/27` 为准，技术合同以 `docs/25` 为准。

## P13 ✅

```text
P13 PASS
TARGET_ASSETS = AVAILABLE
```

最终状态以 `docs/31` 为准；技术与质量合同以 `docs/28`、`docs/29`、`docs/30` 为准。

---

# 5. P13 最终正式基线

正式运行 / 内容合同：

```text
replica-target-assets@1.1.0
p13-replica-target-assets-v3
TARGET_ASSETS schema 1.0
replica-target-visual-identity-v2
target-bible-entity-binding-v1
human-target-asset-approval-v1
review_language = zh-CN
review_language_contract = zh-cn-human-review-asset-local-visual-v2
```

正式硬输入只有：

```text
CURRENT TARGET_BIBLE
```

P13 继续复用 P11 已建立的 Target Character / Scene / Prop ID，不创建平行 identity。`target_asset_id` 由 project + asset type + Target entity ID + binding contract 确定性生成。

当前工程与验收基线包含：

- Character / Scene / Prop typed visual identity packet；
- stable `target_asset_id`；
- 单资产 `target_asset_revision` 与 fingerprint；
- Target Bible semantic identity 的确定性绑定；
- text-only Ark / local vLLM Provider；
- Provider strict schema + exact entity coverage；
- `ProviderJob` 先持久化、再执行外部请求；
- 中文 review-language fail-closed 门禁；
- Provider 成功后只生成 `NEEDS_REVIEW` candidate；
- 显式 ACCEPT / REJECT；
- ACCEPT 后才原子发布正式 `TARGET_ASSETS` Artifact；
- Artifact Graph lineage / SUPERSEDES / stale propagation；
- 普通重复生成幂等、显式 regenerate 推进 generation sequence；
- retry / resume generation checkpoint；
- GET 只读；
- asset-local visual continuity，不复制全局故事 / 镜头 / 对白 preservation locks；
- Character wardrobe 只做 baseline，不擅自建立逐 Scene / Shot 换装计划；
- Scene time-of-day 只做视觉基线，不凭 Scene 顺序、闪回或情绪发明剧情时间；
- legacy visual-identity-v1 正式资产只保留历史，不静默改写。

当前仓库仍**没有已验收的参考图生成 Provider**：

```text
reference_media = []
```

不得伪造图片 URI、hash、尺寸或远端图片 ProviderJob。

---

# 6. P13 真实验收演进记录

P13 的真实 Provider / review / publication 路径已经执行：

```text
CURRENT TARGET_BIBLE
→ real ProviderJob
→ NEEDS_REVIEW candidate
→ USER EXPLICIT ACCEPT
→ CURRENT TARGET_ASSETS
```

旧 `replica-target-visual-identity-v1` 在首次真实人工审核中暴露了三个合同级问题：全局叙事 continuity 注入 asset packet、Character wardrobe 越过 baseline 形成逐场景换装表、Scene time-of-day 无依据发明剧情时间。该轮不 PASS，详见 `docs/30`。

随后仓库完成 v3 / visual-identity-v2 整改：Provider 使用 P13 visual projection、review-facing continuity 限定 asset-local scope、wardrobe / time-of-day 明确 fail-closed 边界、旧 pending candidate superseded、legacy formal asset 不静默改写。整改工程与 main CI 均通过。

用户随后完成同一真实 REPLICA 项目的 v3 复验，并明确确认：

```text
P13 PASS
```

因此旧 `BLOCKED / RERUN REQUIRED / TARGET_ASSETS = PLANNED` 口径已经由 `docs/31` 正式取代。

---

# 7. 当前唯一下一工程动作

当前**不直接实现 P14**。先建立下一阶段正式编号合同。

Root Replica Skill 在 `target_assets` 后的长期内部步骤是：

```text
voice_timing
requires: TARGET_SCRIPT
capabilities: TTS + TIMING
produces: TARGET_AUDIO + TIMING_PLAN
```

该 Root Skill 顺序只是长期架构声明，不等于 P14 已经正式准入。下一阶段合同至少必须明确：Target Voice / Speaker binding、TTS Provider、TARGET_AUDIO typed schema、Actual Speech Duration 权威来源、TIMING_PLAN typed schema、Target Script 与 Target identity 的输入关系、节奏适配边界、Artifact Graph / revision / stale、Provider retry/resume/idempotency、人工听审，以及不得提前进入 Storyboard / Video Generation / QC / Lip Sync / Post。

在新 P14 合同完成前：

```text
P14 implementation = NOT STARTED
TTS = PLANNED
TIMING = PLANNED
STORYBOARD = PLANNED
VIDEO_GENERATION = PLANNED
QC_SELECTION = PLANNED
LIP_SYNC = PLANNED
POST_PRODUCTION = PLANNED
```

---

# 8. 当前正式状态摘要

```text
P10 PASS / SOURCE_SNAPSHOT = AVAILABLE
P11 PASS / LOCALIZATION = AVAILABLE / TARGET_BIBLE = AVAILABLE
P12 PASS / TARGET_SCRIPT = AVAILABLE
P13 PASS / TARGET_ASSETS = AVAILABLE

P14+ = PLANNED / NOT STARTED
下一步 = 先建立正式 P14 合同，再决定实现切片
```
