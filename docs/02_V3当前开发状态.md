# AI Drama Studio V3 当前开发状态

> 更新时间：2026-09-09  
> 当前产品编排基线：Source Understanding Orchestration  
> Seko 产品实测约束：`docs/05_Seko源作概览画布节点实测.md`

---

# 当前阶段

```text
P0 仓库重建基线            ✅ 完成
P1 新工程骨架              ✅ 完成
Seko Skill 架构研究        ✅ 完成并持续补充真实产品实测
P2 Project + Skill Kernel  ✅ 完成
P3 SourceAsset + 输入系统  ✅ 完成
P4 Task / ProviderJob      ✅ 完成
P5 镜头技术锚点            ✅ 完成（原片理解内部能力）
P6 Source Evidence         ✅ 完成（真实短剧人工验收通过）
P7 整集多模态原片理解      🟡 工程实现 / CI 收口中，真实模型人工验收待完成
P8 逐镜精细拉片            ⏸ 未开始
```

P6 已完成工程实现、自动测试与用户真实短剧人工验收，`SOURCE_DIALOGUE_EVIDENCE` capability 已为 `AVAILABLE`。

P7 已进入工程实现阶段，`SOURCE_BIBLE《源作概览分析》`、revision / provenance / CURRENT / STALE、编辑新 revision、Story / Rhythm 派生、真实 Provider 适配和项目级模型选择均已落入 `main`。**但 P7 尚未完成真实短剧 + 真实模型人工验收，因此不能标记完成，`EPISODE_UNDERSTANDING` / `STORY_RHYTHM` 仍不得切换为 AVAILABLE。**

P8 没有开始。

---

# 当前最重要的产品架构

完整 Episode 永远是 Source Truth：

```text
SOURCE_VIDEO / 完整 Episode
= Source Truth
```

视频类项目内部编排：

```text
完整 Episode
        │
        ↓
Media Preflight
        │
        ├────────────────┬────────────────┐
        ↓                ↓                ↓
Shot Anchors         ASR Evidence      OCR Evidence
        │                │                │
        └────────────┬───┴────────────────┘
                     ↓
              Source Evidence
                     +
               完整 Episode
                     ↓
          整集多模态原片理解 / P7
                     ↓
      SOURCE_BIBLE《源作概览分析》
                     ↓
           Story / Rhythm Skeleton
                     ↓
      带全局知识 + Shot Anchors 做逐镜精细拉片 / P8
```

硬规则：

- Shot Boundary 读取完整 `SOURCE_VIDEO`；
- P6 ASR 读取完整连续音轨，不能按 Shot / Reference Clip 分开识别；
- P6 OCR 扫描完整视频时间轴，Shot Anchors 只能辅助抽帧 / 去重；
- P6 Source Evidence 与 P7 Source Understanding 严格分离；
- P7 必须读取完整 Episode，不允许把每个 Shot 独立丢给模型猜剧情后再拼接；
- P7 对白 / OCR 的文字事实必须引用 CURRENT P6 Evidence；模型不得覆盖 canonical Source Evidence；
- P8 必须在 CURRENT SOURCE_BIBLE 已建立后再开始。

工程实现切片 P5/P6/P7/P8 不等于普通用户必须逐项点击的业务流程。普通用户产品流程仍应表现为：

```text
上传完整原片
→ 原片理解
→ 可读、可编辑的《源作概览分析》
→ 后续目标版本创作
```

---

# SOURCE_BIBLE 的正式语义

`ArtifactType.SOURCE_BIBLE` 正式定义为：

> **《源作概览分析》：整集原片理解生成的用户可读、可编辑、可版本化 Source Understanding Artifact。**

第一版正式内容包括：

```text
SOURCE_BIBLE
├─ 素材基线
├─ 整体分析
├─ 时间化原作剧情
├─ 人物与人物关系
├─ 场景
├─ 关键道具
├─ Story / 关键事件
├─ 情绪时间线
├─ Story Skeleton
├─ Rhythm Skeleton
└─ Source Evidence provenance refs
```

它不等于：

```text
ASR raw output
OCR raw output
SHOT_ANCHORS
Provider 原始 response
聊天消息
```

Seko 产品实测观察到完整短剧经多模态分析后可形成画布文本节点《源作概览分析》，包含素材基线、整体分析、完整时间化原剧本、角色关系、场景和关键道具。V3 吸收该产品体验，但数据库正式真相仍由 Artifact Graph / SourceBibleRevision 管理。

---

# P7 已实现的工程范围

## 1. 输入门禁

P7 只对：

```text
REPLICA
REDRAW
```

开放，不对 `TRANSLATION` 执行 SOURCE_BIBLE。

创建 P7 Task 前必须满足：

```text
CURRENT SOURCE_VIDEO
+
CURRENT SOURCE_DIALOGUE / Source Evidence
+
Source Evidence complete = true
+
Evidence Episode 集合与完整 Source Truth 一致
+
可选 CURRENT SHOT_ANCHORS（必须与当前 SOURCE_VIDEO 对应）
```

Shot Anchors 缺失不阻塞 P7。

每个 Episode 调用 Provider 时传入的是 immutable `source.mp4` 完整路径，不是 Shot Reference Clip。

## 2. SOURCE_BIBLE schema

已建立强类型 Pydantic schema，覆盖：

- Material Baseline；
- Overall Analysis；
- timed story segments；
- characters / states；
- relationships；
- scenes；
- props；
- story events；
- emotion beats；
- Story Skeleton / Story Beats；
- Rhythm Skeleton / Rhythm Phases。

服务端负责固定媒体事实（Episode ID / order、原始文件名、duration、宽高、aspect ratio、fps、codec、has_audio），模型不能改写这些 Source Truth。

模型输出还要经过服务端校验：

- 所有时间范围必须在 Episode 内；
- Evidence ID 必须真实属于当前 Episode 的 CURRENT Evidence；
- character / relationship 引用必须一致；
- 非法结构 / 幻觉 Evidence ID fail closed，不发布 SOURCE_BIBLE。

## 3. revision / fingerprint / provenance

P7 Task fingerprint 当前包含：

```text
P7 task/profile/schema version
SOURCE_VIDEO artifact id + fingerprint
SOURCE_DIALOGUE artifact id + fingerprint
可选 SHOT_ANCHORS id + fingerprint
Episode immutable asset SHA / duration
SourceEvidenceSet id + fingerprint
可选 ShotBoundarySet id
实际 Provider profile / model selection
```

因此切换模型属于正式输入变化，不会复用另一个模型的旧业务 Task。

SOURCE_BIBLE provenance 记录：

- exact upstream Artifact ID / fingerprint；
- Episode Evidence Set ID / fingerprint；
- ProviderJob ID；
- provider / model；
- remote response/job ID（存在时）；
- prompt / schema version；
- generated task ID。

API Key 不写入 Project、Artifact、ProviderJob 或 provenance。

## 4. Artifact Graph / 编辑行为

模型成功并通过校验后发布：

```text
SOURCE_VIDEO ─DERIVED_FROM→ SOURCE_BIBLE
SOURCE_DIALOGUE ─DERIVED_FROM→ SOURCE_BIBLE
SHOT_ANCHORS ─USES→ SOURCE_BIBLE       （可选）
SOURCE_BIBLE ─CONTAINS→ STORY_SKELETON
SOURCE_BIBLE ─CONTAINS→ RHYTHM_SKELETON
```

完整 SOURCE_BIBLE 正文存储在 `SourceBibleRevision`，Artifact metadata 只保存摘要 / 指针，不把整篇正文塞进 metadata。

用户编辑 CURRENT SOURCE_BIBLE 时：

```text
SOURCE_BIBLE rev1
↓ 编辑
SOURCE_BIBLE rev2
```

- rev1 保留并变 STALE；
- rev2 成为 CURRENT；
- 旧 Story / Rhythm 跟随 STALE；
- 新 Story / Rhythm 从 rev2 重新物化；
- canonical P6 dialogue / OCR 不发生写入或改写；
- `SUPERSEDES` 关系保留 revision 链。

---

# P7 三模型选择

P7 当前固定提供三个用户可选模型档位：

```text
A. Doubao Seed 2.1 Pro
   → 火山引擎 Ark API
   → 云端生产档

B. Qwen3.8-27B
   → 本地 / 局域网 vLLM OpenAI-compatible
   → 本地高质量档

C. Qwen3-VL-8B-Thinking
   → 本地 / 局域网 vLLM OpenAI-compatible
   → 低显存兼容档
```

项目工作区已有 P7 模型设置 UI，选择存储在 Project 级字段 `source_understanding_provider`。

用户切换模型后：

```text
当前 SOURCE_BIBLE → STALE
旧 STORY_SKELETON → STALE
旧 RHYTHM_SKELETON → STALE
P6 Source Evidence → 仍 CURRENT
```

然后必须显式重新运行 P7。

密钥和本地模型服务地址属于后端 runtime config，不从浏览器保存：

- Doubao：`AI_DRAMA_P7_DOUBAO_*`；
- Qwen3.8-27B：`AI_DRAMA_P7_QWEN38_LOCAL_*`；
- Qwen3-VL-8B-Thinking：`AI_DRAMA_P7_QWEN3_VL_8B_LOCAL_*`。

两个本地模型默认使用独立 vLLM endpoint（8000 / 8001）；如果以后有多模型网关，也可以将两个 BASE_URL 配置到同一个网关。

本地 vLLM 必须能够访问 backend 的完整 Episode Artifact 路径，并允许 local media file URL。模型权重、GPU tensor parallel、量化与 max-model-len 由部署环境决定，不塞进 FastAPI 进程。

---

# P7 Provider 调用契约

## Doubao Seed 2.1 Pro

当前适配器路径：

```text
完整 Episode
→ Volcengine Ark Files API
→ 等待文件可用
→ Responses API input_video + P7 prompt / CURRENT Evidence
→ JSON output
→ Pydantic / Evidence / time-range 校验
→ SOURCE_BIBLE
→ best-effort 删除远端临时文件
```

ProviderJob 必须在远端调用前 commit。

## 本地 Qwen

当前适配器路径：

```text
完整 Episode 本地 file:// URI
+
P7 prompt / CURRENT Evidence
→ vLLM OpenAI-compatible /chat/completions
→ video_url
→ JSON output
→ Pydantic / Evidence / time-range 校验
→ SOURCE_BIBLE
```

Thinking 模型如果 runtime 暴露 `<think>...</think>`，服务端只提取最终 JSON，不将推理文本写入 SOURCE_BIBLE。

---

# P7 前端验收入口

项目工作区当前包含：

```text
P7 模型设置（三选一）
+
P7 开发验收 · 源作概览分析
```

页面打开 / 刷新只做 GET，不自动调用模型。

用户必须显式点击：

```text
运行整集多模态理解
```

结果页展示：

- SOURCE_BIBLE status / revision；
- Material Baseline；
- Overall Analysis；
- 时间化原作剧情；
- 对 P6 Evidence ID 的正文反查；
- 人物 / 关系；
- 场景 / 道具；
- Story / Emotion；
- Rhythm；
- provenance / revision；
- 编辑并保存新 revision。

---

# P7 当前验收状态

自动测试覆盖了：

- GET 无副作用；
- 无 CURRENT Source Evidence 时拒绝启动；
- Provider 收到完整 immutable Episode；
- ProviderJob 在 remote call 前已存在并为 RUNNING；
- Shot Anchors 可选；
- hallucinated Evidence ID 不允许发布；
- SOURCE_BIBLE / Story / Rhythm typed Artifact 发布；
- 编辑产生新 revision 并使旧依赖 STALE；
- P6 canonical Evidence 在 P7 编辑 / Provider 切换后保持不变；
- 模型切换使当前 SOURCE_BIBLE / Story / Rhythm STALE；
- Translation 项目不能执行 P7；
- 前端只展示三个正式模型选项。

当前 CI 正在对 P7 三模型接入的最新提交做收口；已确认 Ark runtime 可在 Python 3.12 安装、FastAPI app 可 import。SQLite provider-choice migration 的 `ALTER COLUMN DROP DEFAULT` 兼容问题已按 Actions 日志修正，需等待最新 CI 重新验证。

**尚未完成的关键验收：真实 Provider + 用户真实短剧人工验收。**

在用户明确确认真实短剧验收通过以前：

```text
P7 != 完成
EPISODE_UNDERSTANDING != AVAILABLE
STORY_RHYTHM != AVAILABLE
P8 不开始
```

---

# P0-P6 已完成基线摘要

## P1

FastAPI / SQLAlchemy / Alembic / pytest 与 Vue 3 / TypeScript / Pinia / Vite / Vitest 工程骨架已建立并由 V3 CI 验收。

## P2

Project + Root Skill / Manifest + Capability Registry + Artifact Graph + ProjectExecutionPlan + revision/fingerprint/CURRENT/STALE 已完成。

## P3

SourceAsset / Episode、多视频上传、ffprobe、decode preflight、immutable raw source、SOURCE_VIDEO revision / fingerprint 已完成。

## P4

Task / ProviderJob / Guardrail 已完成：显式 Command、queue/worker、heartbeat/checkpoint、retry/cancel/resume、ProviderJob-before-remote、GET read-only、STALE fail-closed。

## P5

完整 Episode → Shot Boundary → thumbnail / browser-playable Reference Clip → SHOT_ANCHORS 已完成。用户真实约 1:06 竖屏短剧得到 28 个 CURRENT Shot Anchors 并人工验收通过。

## P6

完整 Episode 连续 ASR + 完整时间轴 OCR → canonical Source Evidence 已完成。用户真实短剧人工验收通过，`SOURCE_DIALOGUE_EVIDENCE=AVAILABLE`。

---

# P8 逐镜精细拉片

P8 仍未开始，必须等待 P7 人工验收确认。

正式输入方向：

```text
完整 Episode
+
CURRENT SOURCE_BIBLE《源作概览分析》
+
CURRENT SHOT_ANCHORS
+
需要时读取 canonical Source Evidence
↓
逐镜精细拉片
```

禁止重新退回“每个 Shot 独立猜整集剧情”。

---

# 当前下一步

```text
1. 等待并修到最新 main CI 全绿
2. 配置真实 Doubao Ark API，跑用户真实短剧 P7
3. 配置本地 Qwen3.8-27B / Qwen3-VL-8B-Thinking vLLM，至少完成一条真实本地模型验收
4. 对同一 Episode / 同一 P6 Evidence / 同一 SOURCE_BIBLE schema 做人工质量对比
5. 用户明确确认 P7 通过后，才允许切 capability AVAILABLE
6. 仍然不进入 P8，直到第 5 步完成
```

继续开发或验收前必须同时读取：

```text
docs/03_Seko3.0_Skill架构逆向分析.md
docs/04_阶段人工验收规范.md
docs/05_Seko源作概览画布节点实测.md
```
