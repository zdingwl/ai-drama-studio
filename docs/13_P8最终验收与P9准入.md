# P8 最终验收与 P9 准入

> 日期：2026-09-10  
> 状态：P8 最终 PASS；P9 可开始，但本文件不包含 P9 实现。  
> 优先级：高于 `docs/08~12` 中所有“P8 待验收 / SHOT_BREAKDOWN = PLANNED / 禁止进入 P9”的历史门禁描述。

---

# 1. P8 最终结论

P8「带 Source Bible 的逐镜精细拉片」已完成工程、真实 Provider、真实数据库一致性和最终人工逐镜验收。

正式能力状态：

```text
SHOT_BREAKDOWN = AVAILABLE
```

正式 P8 契约继续保持：

```text
shot-breakdown@1.1.0
p8-shot-breakdown-v2
SOURCE_SHOT_FACTS schema 1.1
source-bible-shot-facts-v2
```

没有因为最终验收而改变 P8 schema / Provider / Prompt。

---

# 2. 最终真实验收基线

真实项目：

```text
project_id = 99f7036f-cce8-4700-a717-683278107866
Episode = 货到付款惩治隔壁大妈-第01集.mp4
时长 = 66.36 秒
Shot = 28
```

最终 CURRENT 链：

```text
P5 SHOT_ANCHORS          rev1 CURRENT
P6 SOURCE_DIALOGUE       rev9 CURRENT
P7 SOURCE_BIBLE          rev8 CURRENT
P8 SOURCE_SHOT_FACTS     rev6 CURRENT
```

最终 P7 Scene candidates：

```text
scene_003 王桂香家客厅       00:00.000 → 00:00.800
scene_001 居民楼公共楼道     00:00.800 → 00:22.000
scene_002 徐然家客厅         00:22.000 → 01:06.360
```

最终 P8 Scene binding：

```text
Shot #001       王桂香家客厅
Shot #002-#009  居民楼公共楼道
Shot #010-#028  徐然家客厅
```

Scene binding 异常数量：0。

---

# 3. 最终一致性验收

最终真实 P8 已确认：

- 28/28 Shot `shot_anchor_id / start_us / end_us / duration_us` 与 CURRENT P5 完全一致；
- P8 dialogue text 与 CURRENT P6 canonical dialogue 逐字差异为 0；
- P8 speaker candidate 不引用非 CURRENT P7 character；
- P8 scene candidate 不引用非 CURRENT P7 scene；
- character / scene / prop candidate 均受 CURRENT SOURCE_BIBLE 边界约束；
- Artifact Graph 必需关系缺失为 0；
- fingerprint / provenance / SUPERSEDES 正确；
- ProviderJob 在真实外部调用前已持久化；
- 重复 GET 前后 Artifact / Task / ProviderJob 数量不变；
- P7 / P8 相关自动测试最终复验通过；
- 最终用户人工确认 Shot #001 的真实场景为“王桂香家客厅”，P7 修正后 P8 重跑结果正确。

因此 P8 不再有阻塞 P9 的已知架构问题。

---

# 4. P8 冻结边界

进入 P9 后，P8 继续是 Source Facts 的逐镜分析层，不得被 P9 反向改写。

固定权威关系：

```text
完整 Episode / SOURCE_VIDEO          = Source Truth
CURRENT P5 SHOT_ANCHORS              = Shot 时间权威
CURRENT P6 canonical Source Evidence = 对白 / OCR 文字权威
CURRENT P7 SOURCE_BIBLE              = 故事、人物、关系、场景、道具全局候选知识
CURRENT P8 SOURCE_SHOT_FACTS          = 逐镜视觉、镜头语言、candidate binding、delivery / sound facts
```

P8 speaker candidate 仍是 provisional candidate，不等于 P9 最终 Speaker Truth。

P9 不得：

- 修改 P5 Shot 边界；
- 重新听写并覆盖 P6 canonical dialogue；
- 用最终身份归一结果回写历史 P7/P8 revision；
- 把 Reference Clip 当完整 Episode Source Truth；
- 把 P8 candidate binding 直接当最终 identity 而跳过 P9 证据归一。

---

# 5. P9 正式准入

P9 现在允许开始。

规划中的 P9 范围：

```text
Speaker / Character / Scene / Prop 最终归一
```

当前 Capability 起点：

```text
IDENTITY_RESOLUTION = PLANNED
SCENE_RESOLUTION    = PLANNED
PROP_RESOLUTION     = PLANNED
SOURCE_SNAPSHOT     = PLANNED
```

P9 只开发前三类归一能力及其必要 typed domain / Artifact；不得提前完成 P10 `SOURCE_SNAPSHOT`。

P9 开发前必须先设计并写文档，至少明确：

- Professional Skills：character-resolution / speaker-attribution / scene-resolution / prop-resolution 的职责边界；
- P7 candidate 与 P8 shot binding 如何转成稳定 Source identity；
- Speaker 与 Character 的关系，以及无法确认时的 UNKNOWN / unresolved 规则；
- 跨 Shot / 必要时跨 Episode 的 identity evidence；
- Scene / Prop 的合并、拆分、同名不同实体规则；
- typed schema / domain model / Artifact / revision / fingerprint / provenance；
- CURRENT / STALE 传播；
- Artifact Graph 关系；
- GET 只读、真实重任务显式 POST、ProviderJob 先持久化；
- 人工确认 / 修改与 revision 规则；
- P9 人工验收规范。

必须先设计，再实现 Provider 和 UI。

---

# 6. P9 硬输入

P9 至少以以下 CURRENT Source 层为输入：

```text
CURRENT SOURCE_VIDEO / 完整 Episode
CURRENT SOURCE_BIBLE
CURRENT SOURCE_SHOT_FACTS
CURRENT SHOT_ANCHORS
CURRENT canonical Source Evidence（需要核对 speaker / OCR / dialogue 时）
```

完整 Episode 仍是最终 Source Truth。

P9 不允许每个 Shot 独立做最终身份判断后直接拼接；必须利用跨 Shot / 全集上下文和已有 P7/P8 candidate evidence 做归一。

---

# 7. P9 完成门禁

P9 只有在以下条件全部满足后才可判完成：

- typed schema / Artifact / provenance / fingerprint / revision 完成；
- identity / scene / prop 的 fail-closed 规则完成；
- GET / POST / ProviderJob 约束完成；
- 自动测试通过；
- 同一真实短剧完成真实 Provider / 真实数据验收；
- 用户完成人工验收；
- 不存在把 provisional P7/P8 candidate 冒充最终 identity 的路径。

在此之前：

```text
IDENTITY_RESOLUTION / SCENE_RESOLUTION / PROP_RESOLUTION 保持 PLANNED
```

P9 完成后才允许进入 P10 `SOURCE_SNAPSHOT`。
