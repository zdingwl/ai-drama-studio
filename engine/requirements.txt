# AI Drama Studio Reference Video V2 后端依赖。
fastapi==0.128.2
uvicorn==0.48.0
pydantic==2.13.4
SQLAlchemy==2.0.50
alembic==1.18.4
python-multipart==0.0.29
httpx==0.28.1
pytest==9.0.2

# F04 本地 TransNetV2 Shot Boundary Detection。
transnetv2-pytorch==1.0.5
torch==2.5.1
numpy==2.1.3
pandas==2.2.3
pillow==11.1.0
tqdm==4.67.1
ffmpeg-python==0.2.0
future==1.0.0

# F05 人物视觉证据：YuNet + SFace 通过 OpenCV DNN 本地运行。
opencv-python==4.11.0.86

# F05 源对白 ASR。默认模型由 AI_DRAMA_WHISPER_MODEL 控制，首次使用需要准备对应模型缓存。
faster-whisper==1.2.1

# Speaker Diarization 当前是可选能力：只有设置 AI_DRAMA_DIARIZATION_MODEL_PATH 且本机
# 已安装兼容的 pyannote.audio 时才启用。默认 requirements 不强制安装 gated 模型依赖。
