# F01–F05 当前实际使用的后端依赖。
# 新依赖必须按 Feature 追加并锁定版本；禁止依赖 latest。
# F05 三栏镜头工作台只复用 FastAPI / SQLAlchemy / FFmpeg，不新增第三方 Python 包。
fastapi==0.128.2
uvicorn==0.48.0
pydantic==2.13.4
SQLAlchemy==2.0.50
alembic==1.18.4
python-multipart==0.0.29
httpx==0.28.1
pytest==9.0.2

# F04 本地 TransNetV2 Shot Boundary Detection。
# torch 版本单独锁定，避免 transnetv2-pytorch 的 torch>=1.9.0 在新机器上漂移。
transnetv2-pytorch==1.0.5
torch==2.5.1
numpy==2.1.3
pandas==2.2.3
pillow==11.1.0
tqdm==4.67.1
ffmpeg-python==0.2.0
future==1.0.0
