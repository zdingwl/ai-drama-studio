# AI Drama Studio Reference Video V2 后端依赖。
fastapi==0.128.2
uvicorn==0.48.0
pydantic==2.13.4
SQLAlchemy==2.0.50
alembic==1.18.4
python-multipart==0.0.29
httpx==0.28.1
pytest==9.0.2

# 02 拉片 V4：TransNetV2 提候选，PySceneDetect 提供第二证据，最终回到 Source PTS 帧级精修。
transnetv2-pytorch==1.0.5
scenedetect==0.6.7.1
torch==2.5.1
numpy==2.1.3
pandas==2.2.3
pillow==11.1.0
tqdm==4.67.1
ffmpeg-python==0.2.0
future==1.0.0

# 03 人物 V6：YOLOX Person + 成熟 MOT + Global Identity Resolver。
# trackers 2.6.0 提供 ByteTrack / BoT-SORT；supervision 负责标准 Detections 数据结构。
# 当前 Face provider 继续使用已校验的 YuNet/SFace；V6 已把 Face provider 与 Global Identity 解耦，
# 后续可以替换成有明确商用授权的 ArcFace 权重，而不改变 Track / Final Asset 数据结构。
trackers==2.6.0
supervision==0.30.0
opencv-python==4.11.0.86
onnxruntime-gpu==1.20.2

# F05 源对白 ASR。默认模型由 AI_DRAMA_WHISPER_MODEL 控制，首次使用需要准备对应模型缓存。
faster-whisper==1.2.1

# Speaker Diarization 当前是可选能力：只有设置 AI_DRAMA_DIARIZATION_MODEL_PATH 且本机
# 已安装兼容的 pyannote.audio 时才启用。默认 requirements 不强制安装 gated 模型依赖。
