# AI Drama Studio Reference Video V2 后端依赖。
fastapi==0.128.2
uvicorn==0.48.0
pydantic==2.13.4
SQLAlchemy==2.0.50
alembic==1.18.4
python-multipart==0.0.29
httpx==0.28.1
pytest==9.0.2

# 02 拉片 V4：TransNetV2 提候选，PySceneDetect 提供第二证据，最终回到 Source PTS 帧级精修。
transnetv2-pytorch==1.0.5
scenedetect==0.6.7.1
torch==2.5.1
numpy==2.1.3
pandas==2.2.3
pillow==11.1.0
tqdm==4.67.1
ffmpeg-python==0.2.0
future==1.0.0

# 03 人物 V10.1：YOLOX Person + Mature MOT + YoutuReID Person Classification。
trackers==2.6.0
supervision==0.30.0
opencv-python==4.11.0.86
# ORT 1.21.x 使用 CUDA 12.8 + cuDNN 9，并提供 preload_dlls()；[cuda,cudnn]
# 同时安装 NVIDIA Python runtime DLL，Windows 不再依赖系统 CUDA Toolkit 才能做人脸/人物 GPU 推理。
onnxruntime-gpu[cuda,cudnn]==1.21.1

# Breakdown P2.2 本地 ASR Provider。正式入口使用 AI_DRAMA_P2_ASR_* 配置；
# 默认质量模型为 large-v3，首次使用需要准备对应模型缓存。旧 F05 helper 继续兼容旧 AI_DRAMA_WHISPER_* 配置。
faster-whisper==1.2.1

# Speaker Diarization 当前是可选能力：只有设置 AI_DRAMA_DIARIZATION_MODEL_PATH 且本机
# 已安装兼容的 pyannote.audio 时才启用。默认 requirements 不强制安装 gated 模型依赖。
