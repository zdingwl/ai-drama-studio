# AI Drama Studio Reference Video V2 后端依赖。
fastapi==0.128.2
uvicorn==0.48.0
pydantic==2.13.4
SQLAlchemy==2.0.50
alembic==1.18.4
python-multipart==0.0.29
httpx==0.28.1
pytest==9.0.2

# 02 拉片 V4：TransNetV2 提候选，PySceneDetect 提供第二证据，最终回到 Source PTS 帧级精修。
transnetv2-pytorch==1.0.5
scenedetect==0.6.7.1
torch==2.5.1
numpy==2.1.3
pandas==2.2.3
pillow==11.1.0
tqdm==4.67.1
ffmpeg-python==0.2.0
future==1.0.0

# 03 资产人物视觉：YuNet / SFace 继续走 OpenCV CPU；
# YOLOX Person Detection + YoutuReID 默认 ONNX Runtime CUDA，CUDA 不可用自动 CPU fallback。
opencv-python==4.11.0.86
onnxruntime-gpu==1.20.2

# F05 源对白 ASR。默认模型由 AI_DRAMA_WHISPER_MODEL 控制，首次使用需要准备对应模型缓存。
faster-whisper==1.2.1

# Speaker Diarization 当前是可选能力：只有设置 AI_DRAMA_DIARIZATION_MODEL_PATH 且本机
# 已安装兼容的 pyannote.audio 时才启用。默认 requirements 不强制安装 gated 模型依赖。
